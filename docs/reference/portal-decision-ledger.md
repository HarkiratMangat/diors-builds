---
kind: reference
status: live
---

# Portal decision ledger — what is already settled

> 🔴 **THIS FILE IS QUERIED, NOT READ — AND `rg` IS THE WRONG TOOL FOR IT.**
>
> ```
> ctx_search(queries: ["has <the surface> been decided", "is <X> a decision"],
>            source: "/Applications/Claude Code/Diors-Builds/docs/reference/portal-decision-ledger.md")
> ```
>
> ⚠️ **MEASURED 2026-08-31 12:5x EDT, because the first draft of this header said "rg it" and that was wrong.** Against the six selectors an audit finding actually prints — `sclock` · `span.done` · `b.t-legendary` · `countdownParts` · `mh-take` · `ow-i` — **`rg` found 1 of 6.** The same five questions asked in prose through `ctx_search` returned **5 of 5**, each landing on the right row. `b.t-legendary` misses on `rg` because this file writes `.t-legendary` and the finding prints the `b` prefix: **a ledger is prose, a finding is a literal, and `rg` cannot bridge them.**
>
> 🔴 **`feedback_token_conscious_tool_routing` already said this and I did not apply it: a QUESTION about prose → `ctx_search`; a known STRING → `rg`.** The memory records `rg` finding 0/4 on exactly this shape.
>
> ⚠️ **PRECONDITION — INDEX IT ONCE, EVER. NOT once per session.** ✅ **Measured 2026-08-31 13:0x EDT: `ctx_search` printed `Auto-refreshed 1 stale source (file changed since indexing)` after the file was edited**, so it self-maintains and a stale index is not a hazard. **The only hazard is a file that was NEVER indexed — it returns nothing, and that is indistinguishable from a file with no matching row.** If a query comes back empty and you are not certain this file has ever been indexed, run `ctx_index(path: "…/portal-decision-ledger.md")` and ask again before believing the silence.
>
> **Batch it with the audit** — `ctx_batch_execute` runs the realm's audit and queries this file in one round trip, which is the moment both are needed.
>
> It exists because on 2026-08-31 I probed the season clock, the accent tokens and `dateOnly` — **three decisions whose answers were already written three lines from the code** — and re-derived all three. It exists because on 2026-08-31 I probed the season clock, the accent tokens and `dateOnly` — **three decisions whose answers were already written three lines from the code** — and re-derived all three.
>
> ⚠️ **IT ANNOTATES, IT NEVER FILTERS.** A row here means *a decision may cover this — go check it*. It never means *ignore the finding*. The triage classifier built the same day proved the hazard: it could report a false CITED on any `ow-*` row, hiding a real defect on the surface I had made the most decisions about. **A matcher that silently claims "decided" is worse than no matcher.**
>
> **Add a row when a decision is made, not at handoff time.** A decision that lives only in a commit message is invisible to every search over docs. ⚠️ **Write the row in PROSE, not only as a selector** — `ctx_search` matches meaning, so *"the countdown's time source"* is findable where a bare `.sclock` is not.

## How to read a row

🔴 **A ROW WHOSE DIFFERENCE STOPS REPRODUCING IS NOT STILL TRUE — Harkirat, 2026-09-04 20:38 EDT.** *"CITED = report forever, do not chase"* governs a difference that is STILL THERE. When the difference itself disappears, the decision has expired: the row returns to unadjudicated and is decided again against what the pages do now. **It is not deleted** — the record of why it was cited is what stops the same argument being had a third time. Four of Review's cited coordinates are in exactly this state. §0.7c carries it as the fifth triage bucket.

**Surface** — what a finding would name (a selector, a file, a component). **Decision** — what was settled. **Reopens if** — the falsifier: what would have to be true for this to be worth revisiting. A row with no falsifier is dogma, not a decision.

---

## Cross-realm — these bind every realm, including ones nobody has opened

⚠️ **ALWAYS NAME THE INSTRUMENT BESIDE THE NUMBER — the two report different page heights and BOTH ARE RIGHT.** `portalDiff` captures the full page; `portalAudit` walks the innermost `main`. On HOME they read **1243px** and **1199px**; on ARMORY the gap is a systematic **44px**. Three carriers published "both pages 1243px" unattributed until 2026-09-03 23:28 EDT — the two-measurements-neither-labelled defect §0.6a records about itself. 🔴 **HOISTED HERE 2026-09-04 20:02 EDT. It sat under `## Review` quoting HOME's two figures**, so the one general rule in this file was filed as a Review-specific note under the wrong realm's heading; a reader of the Review section met Home's numbers, and a reader of any other section met the rule nowhere.


| Surface | Decision | Date | Why | Reopens if |
|---|---|---|---|---|
| `.t-legendary` `.t-mythic` — **Season's tier chips** | 🔴 **RE-GRADED CITED → FIXED, 2026-09-04 20:16 EDT. The PACKAGE is corrected.** | 2026-08-31 · re-graded 2026-09-04 | `season.html:2423` builds its chip as `t-${t.toLowerCase()}` from the tier WORD, so it emits `t-legendary` and `t-mythic` while the package sheet defined only the abbreviations `.t-leg`/`.t-myth` — `.t-epic` matched by luck. Every Legendary and Mythic chip in the design rendered as unstyled text. ⚠️ **This row's own reasoning — *"a typo in the package, not a design decision"* — is §0.1a test 2 verbatim, and it was used to CORRECT the accent tokens on the same day it was used to LEAVE this one. Two package defects, opposite treatments, one file.** Its falsifier was *"the mockup package is regenerated"*, which nobody was ever going to do: a citation with an untriggerable falsifier is permanent by construction. ⚠️ **The row also said "Armory's tier board" and the emitter is on SEASON.** Measured after: **season 135 → 133 regions, ④ 60 → 53**; no realm regressed | The package sheet stops defining what its pages emit |
| `shell.js` crumb separator · `overlay.js` drawer close | **Portal keeps the inlined SVG.** Class (c) | 2026-08-31 | `reference_never_text_glyphs_for_icons` — a text glyph inherits font metrics nothing here controls. ⚠️ **These two must always resolve together**; an SVG chevron beside a text-glyph close is two habits, not one rule | The standing rule is retired |
| `tokens.css` `--ctl-min` `--ctl-pad` `--ctl-rad` | **Design's values, adopted app-wide** | 2026-08-31 | Every button in every realm. Verified on Season and Broadcast only — **Armory, Access, Analytics and Review were never opened after it** | A realm shows button metrics that look wrong |
| `--draw` `--ret` `--ev` `--play` accent tokens | **Portal keeps its values.** Settled, not a conformance question | before 2026-08-31 | `tokens.css`'s own comment: **the design's stylesheet values never painted anything** — its Track renders from `lane.bar` in the fixtures, which is exactly what the portal's tokens carry. Both sides were internally consistent, which is why nothing caught it until they were rendered side by side | Someone re-measures and finds the fixtures disagree with the tokens |
| `.mh-stats .stat .v.zero` and the `--ink4` group — `.round-u.none` · `.lnh-n` · `.lane.lnc .lnh` · `.cmdbar kbd` · `.att-i` · `.atx` | **Portal keeps `--ink3` wherever the token colours TEXT, and `--ink4` wherever it does not.** Class (b) | 2026-08-31 | ⚠️ **This row used to read "Design's `--ink4`" with no Why and no falsifier, and taken at face value it says to adopt a colour that fails contrast.** The reason is recorded in the portal's own stylesheet, three lines from the code: `portal/ui/app.css`'s own comment on `.trow-k` measures `--ink4` at **3.46:1** (`rg '3.46:1' portal/ui/app.css` — ⚠️ it was cited as `:3411` and is at `:3417` as of 2026-09-03 22:49 EDT; **line numbers into this file rot, so cite the text**), below WCAG AA's 4.5:1 for body text. So the design's `--ink4` stands on non-text (a dot, a rule, a border) and `--ink3` replaces it on anything a person reads. Every selector listed here is text | `--ink4` is re-derived to clear 4.5:1, or a listed selector turns out to colour something that is not text |
| **Redesigns** | **Wait for ALL SIX realms.** Re-affirmed against my own argument | 2026-08-31 | I reasoned the rule existed only because a flag switched redesigns off, and that the collapse deleted the flag. **Harkirat kept it.** ⚠️ **A rule he set does not lapse because I can no longer see its reason** | He says so |
| **What a realm pass is FOR** | **The diff finds SURFACES · I read the CODE · HE closes by looking.** The percentage is a pointer, never a target | 2026-08-31 | Every real defect this pass found came from reading code around a difference; **none came from the number**. Driving the number produced three false closes, a self-comparison sweep and an 888px lie | — |
| **Merge cadence** | **Merge this branch, then one PR per realm** | 2026-08-31 | 281 commits, 53,795 insertions, no PR for six days ⚠️ **The branch is NOT "Season"** — it carries the Preact migration, the instrument suite and the mode collapse | — |
| `?conform=1` | **Gone.** Renamed `?fresh=1`, which does FIXTURES ONLY | 2026-08-31 | The two rendering modes collapsed. **There is no stand-down switch — do not look for one, and do not add a `conforming()` site** | — |
| **375×812** | **Dropped** | 2026-08-30 | His call | He asks for it |
| **THE SUBTRACTION PASS** — six deletions, and every one RAISES the diff | 🔴 **CITED, and the direction is the point.** The mockup draws the duplicates; removing them makes the portal differ from it ON PURPOSE | 2026-09-04 22:58 EDT | Six design reviewers converged on one finding: the machine signature is that nothing is ever taken away. Each deletion below was chosen by a RULE, not by taste, and each costs regions because the design still draws what was removed. **Measured before and after in one sweep:** analytics **14.2% → 12.6%** (④ 120 → 110, ② 130 → 159 as the pairing re-partitions against the mockup's four tiles) · season 135 → **141** · home 27 → **29** · broadcast 17 → **20** · armory 906 → **913** · review 13 → **14** · access unchanged. ⚠️ **A session reading these as regressions will restore the duplication.** They are the cited floor moving up, which is what a deliberate divergence looks like in this instrument | Harkirat says the removed element was carrying something |
| Analytics' four health tiles → **two** | **Portal keeps two.** Class (b) | 2026-09-04 22:58 EDT | The rule, not taste: **a tile whose own subtitle is defined against a masthead figure is a third statement of that figure.** `Distinct users 24h` read *"across N commands"* beside a masthead reading COMMANDS 24H; `Quiet alerts 24h` read *"below the N errors"* beside ERRORS 24H. The code's own comment records that the first version wired all four to Timing *because the design wires its four there* — four slots existed, then four numbers were found for them. Both filters stay reachable (level rows scope the river; Reach is a view tab) | The masthead stops carrying commands and errors |
| Analytics' "Restarts" panel plotted `spark.alerts` | ✅ **FIXED — a mislabel, not a divergence** | 2026-09-04 22:58 EDT | The heading said Restarts, the prose was about restarts, the series was alerts, and the figures could not be reconciled — a tile said **303** restarts in 7d while the chart totalled **6**. `spark.boots` has been in the payload all along (`portal/api/analytics.js:110`). Corrected toward the heading and prose rather than by retitling, because they are the half that is right | — |
| Season's zero-result Repairs cards | **Portal removes them.** Class (b) | 2026-09-04 22:58 EDT | A ~130px card at full weight — heading, MECHANICAL chip, three-line body — printing *"Nothing to fix"* plus a bot source path to an operator. The panel header already says *"16 findings across 6 checks"*, so a check with nothing to report is already counted. Costs season 135 → 141 regions because the mockup still draws them | The findings header stops stating the check count |
| Home's masthead **staged** figure | **Portal removes it on HOME only.** Class (b) | 2026-09-04 22:58 EDT | Home stated one number **four** times: header commit chip, rail badge, this stat, and the resume strip 60px below — the only one of the four that says WHICH realms and what staged costs. On a page titled *What needs you*, whose job is ranking, a bare repeated digit is the weakest. ⚠️ **Home only** — every other realm's staged figure is scoped to that realm | Home stops carrying the resume strip |
| `.trow .bchip` and `.nscard.over` opacity dims | **Portal moves the dim off the text.** Class (b) — an accessibility correction | 2026-09-04 22:58 EDT | `--ink3` on `--raised` is **4.81:1**, which DESIGN.md records as deliberate: solving for four AA-passing ink tones collapses two, so the scale has room for three. A **6%** dim on top of that put **64 text nodes** on Armory's resting Board at 4.42:1, and `.nscard.over`'s 0.62 put Broadcast's over-run card at 2.73–3.21:1 — the one card whose whole job is to be noticed. Hover restored it; hover is not an accessibility remedy. The dim now rides the border and background, which carry no text | `--ink3` is re-derived with real headroom |
| **The staged tray** | 🔴 **BUILT ON EVERY REALM — Harkirat, 2026-09-04 21:28 EDT. It existed nowhere in the portal and the design floats it on every page.** | 2026-09-04 21:28 EDT | Two findings in one place. ① `portal/ui/tray.js` held a DIFFERENT component — a "Saved · Undo" tray for tier-1 direct edits — wired on Season alone, driven by a `notices` list that `season.js` set to `[]` and **nothing anywhere ever added to**: two removal handlers, no producer. It returned `null` on every render it ever had. ② The design's tray is the STAGED one: *Staged · N changes*, a row per op, **Discard all** and **Review & commit**. It is Shell-owned now, so a realm inherits it the way it inherits the rail — a shared surface wired realm by realm is a shared surface five realms forget, which is the `badges` defect in another file. Default COLLAPSED, matching the design's own reason (*a status object… defaulting to the full list put a 269px floating panel over page content*), and `main` reserves `tray height + 34` so it does not cover the foot of the realm — **136.4px against the design's 138.5px**. ⚠️ **Review is deliberately excluded**: a floating *Review & commit* on the Review screen is a control pointing at itself. ⚠️ **A per-row Undo is NOT faked** — the only discard endpoint takes a CHANGESET, so a per-row undo would silently take back the other four ops in it; every row routes to Review, which is the design's own fallback (*"Not a refusal — a route"*). 🔴 **FOUND BY OPENING BROADCAST'S EXPORT DRAWER, and the resting audit never reported it** — the LCS alignment paired it away. The overlay tier found a defect on the resting page. Its cost: broadcast 0.8% → 1.2%, home 0.7% → 1.2%, access 7.1% → 7.3%, review 12 → 13 regions | The design drops the tray |
| `a.hdr-commit` — the header commit chip | **Portal keeps its segmented two-cell chip.** Class (b) — **Harkirat, 2026-09-04 20:43 EDT**, shown both captures | 2026-09-04 20:43 EDT | The design draws one padded pill with a 9px gap; the portal splits it so the figure gets its own cell against a divider and the words get the rest. The portal's own rule carries the reason and it is a real defect in the design's version, not a preference: the count and the words were two loose objects on a 9px gap, so `1` read as a stray pipe beside a sentence. **The design's UNDERLINE is a separate matter and was corrected in the package** — it is an `a` whose rule never set `text-decoration`, so the chrome's anchor styling underlined it (§0.1a test 3). ⚠️ **This surface had never been compared on five of seven realms**, because the chip draws only when something is staged and only Home and Season passed `badges`. Captures: `https://claude.ai/code/artifact/6fd1be3f-d276-4b09-b6af-a16440ffecae` | The design's chip is redrawn as a segmented control |
| `#mhAdd` — Armory's second create chip | 🔴 **THE MOCKUP IS RIGHT AND THE PORTAL WAS BROKEN — Harkirat, 2026-09-04 20:43 EDT. FIXED, not cited.** | 2026-09-04 20:43 EDT | The design's `openAdd(mode)` mounts a build form in that armoury and leaves the rack alone (`mode || st.armMode`). The portal's handler did `setArmMode(m); setShowAdd(true)` — it opened the form AND swapped the rack out from under it, **−4,531 nodes against the design's +117**. `AddBuildForm`'s own comment has stated the distinction since it was written (*"which armory the build is filed under… rather than which armory you are looking at"*) and the handler four hundred lines below it did both. Separated into `addMode`; the two entry points that name no armoury open in the one you are standing in, which is the design's own `mode \|\| st.armMode`. ⚠️ **Every instrument shoots the page AS IT LOADS, so only `--open` could ever have seen this — and the first version of `--open` accepted the collapse as an overlay and exited 0** | The two armouries stop being separate racks || `.bc-mode` — the MP/DMZ label beside a build's name | 🔴 **PACKAGE CORRECTED, 2026-09-04 20:16 EDT.** Class: package defect, §0.1a test 2 | 2026-09-04 | `armory.html:357` emits `.bc-mode` and no sheet in the package defined it, so the word classifying a build as MP or DMZ rendered as body text at body size. The portal's own rule carries the reasoning (*a classification beside a name, not part of the title*) and is adopted verbatim. Measured after: **armory 8.6% → 8.0%, ④ 125 → 111**, and the mockup grew **10541 → 10600px** — the region count rose 894 → 903 because a 59px offset re-partitions the page, which is the ① CASCADE effect and not nine new differences | The package sheet stops defining what its pages emit |
| **The three package corrections' MOVEMENT, isolated** | **Measured, not inferred** | 2026-09-04 20:16 EDT | The badge commit and the package corrections landed in the same working tree, so the sweep would have conflated them. The package was stashed, the tree rebuilt and swept, then restored — two full sweeps. **Badge alone:** armory +2, broadcast +1, access +1, analytics +1, review −1 region; season and home unchanged (both already passed `badges`). **Package corrections alone:** season −2 regions and ④ −7 · broadcast ② −2 and ④ −1 · armory ④ −14 and −0.6% at +9 regions on a +59px page · access, analytics, review, home identical to the pixel. **No realm regressed on ② or ③.** ⚠️ This is the obligation §0.1a's rewrite imposes on any package correction, paid deliberately rather than discovered | — |

### Cross-realm — the first working `--hover` run, 2026-09-04 12:40 EDT

| Surface | Decision | Why | Reopens if |
|---|---|---|---|
| **`--draw` `--ret` `--ev` `--play` `--dw`** — the category accents | **THE PORTAL WAS RIGHT AND THE MOCKUP'S STYLESHEET WAS BEHIND ITS OWN COMPANION. Package corrected.** Class (b) | The package's `assets/tokens.css` carried `#FF3430` / `#337BA6` / `#1F8A5E` / `#8A6BD1` and had no `--dw` at all. **COMPANION §4.2 trap Two records that exact set as the one that produced "two plums and two blues that read as one family"**, and that each lane was moved to a separate hue — draws keep Plum, playlists take Cyber Teal, events take Slate pushed to a true blue, returning takes a rose, minimum hue gap **31°** — ⚠️ **AND THAT NUMBER IS FALSE FOR THESE VALUES, measured 2026-09-04 14:21 EDT.** The six-token set's real minimum is **27.8°** (`--ret`↔`--dw`), 30.6° without `--dw`, and the set it replaced measured **44.0°** — wider. The falsifier this row shipped with fired on first use. `portal/ui/tokens.css` implements the described SYSTEM. So the design's prose and the design's CSS disagreed and the portal agreed with the prose. ⚠️ **Invisible to every resting comparison ever run** — these tokens reach the rail only through `.realm:hover svg{color:var(--c,var(--patch))}`. Corrected: **Armory 893 → 892 regions, ④ 133 → 125; Analytics 41 → 40, ④ 125 → 120; no realm regressed** 🔴 **DECIDED BY HARKIRAT 2026-09-04 20:38 EDT: KEEP THE PORTAL'S SET.** He was shown the trade — 27.8° against the replaced set's 44.0° — and kept the current one; COMPANION §4.2's own 31° claim is corrected to 27.8° in the same change, and its "31° is a floor" rule with it, because a floor stated above a palette that violates it is decoration. **Do not re-derive this.** | A lane's bot source changes |
| **The overlay tier's recorded figures** | **They are PAGE-sized, not overlay-sized. Read the DELTA against the realm's resting audit.** Not a decision — a property of the instrument | `portal:audit --open` re-walks the whole page, so a recorded `access · Export ④ 304` is the realm's own resting ④ 275 plus the drawer's **29**. Measured across all six opened overlays: contributions are ② 9 · ③ 17–30 · ④ 20–42, except armory's create overlay at ② 75. **⑤ RULES is byte-identical on every overlay run** — opening a drawer does not change a stylesheet, so that section carries no overlay information at all | The audit learns to scope its walk to the mounted overlay |
| **The `② 9` that appears in every overlay run** | **Alignment artifact plus one cited row.** Not findings | Opening anything changes the node count, which re-runs the LCS pairing: the whole-page `main`/`div` text node, the view-tab `span`, and the mockup's inline `path`/`circle`/`rect` against the portal's `svg`+`use` sprite (the settled icon-delivery class). The one real member is `span.id` "…2283" — the cited identity row | The aligner stops pairing by sequence |


## Season — closed as a cited floor, not as a clean sheet

| Surface | Decision | Date | Why | Reopens if |
|---|---|---|---|---|
| `section.collapsed.identity.rise` **+16px** | **① CASCADE closed as CITED.** Reports forever | 2026-08-31 | The banner thumbnail is the **broken-image detector** — `brokenBanner` is written by its `onError`. Adopting the design's no-image version lets a broken banner assert *"image cached and serving"*. ⚠️ **A future session WILL see this finding on every Season audit. It is not unfinished work** | The banner surface is redesigned to occupy the design's height |
| `.sclock` — the season clock | **Design's readout on SEASON. 🔴 THE HERO CLOCK WAS NOT DELETED — corrected 2026-09-03 20:18 EDT.** It survives as `ClockFace` (`portal/ui/season.js:236`), exported, styled (the `.sc-hero`/`.sc-heroL`/`.sc-unit`/`.sc-tick` block, since DELETED — the line range once cited here now lands on `.sc-when,.sc-then`), and **`portal/ui/home.js:120` is its only consumer** — so Home still renders attempt 13 while Season renders the design. The deletion was applied to `SeasonClock`'s call site (`season.js:301`) and recorded as applied to the component. ✅ **RESOLVED 2026-09-03 20:29 EDT, Part 6b:** `ClockFace` now emits the design's four-segment readout and `SeasonClock` calls it, so there is ONE face and both realms render it. The hero CSS (`.sc-hero`, `.sc-heroL`, `.sc-unit`, `.sc-tick`, `.sc-t-sep` and their tier rules) is deleted; the design's `.sc-u:first-child b` tiers and its `:nth-last-child(2)` separator rule replace them, and Home gained the design's own `.hclock .sclock{align-items:flex-start;text-align:left}`, which the portal had never defined. Season measured identical before and after (`.sc-face` w=184 both times) | 2026-08-31 · corrected 2026-09-03 20:18 EDT | Attempt 13, COMPANION §16.31a, its own critique never addressed. **Queued for redesign in the deferred list — what comes back is not that code.** Photographed before deletion: `https://claude.ai/code/artifact/48baf822-3a53-46d0-9fe9-93da8e00d104` | The redesign phase begins |
| `SeasonClock`'s time source | **Portal keeps `Date.now()`.** Class (b) | 2026-08-31 | The design reads from the start of its fixture day — that is what made two captures comparable, and **it is not a clock for a running console** | — |
| `oneway.js` — **SEVEN operations, not five** | **Portal keeps all seven and refuses the design's wording.** Class (b) ×4 | 2026-08-31 | Conforming would delete two real destructive ops, promise an **export interlock this console does not have** (`portalExport.test.js:147` forbids exactly that claim), and **remove the typed-confirm field in front of seven irreversible operations** | The changeset-export interlock moves to this screen |
| Day drawer's prev/next buttons + *Open a day…* | **Portal keeps them.** Class (b) | 2026-08-31 | The entire keyboard path into that surface. 🔴 **§0.6a's own coverage note says the overlay method cannot see keyboard reachability** — nothing downstream would ever have reported this | — |
| Calendar-banner status line | **Portal keeps its wording and its `<img>`.** Class (b) | 2026-08-31 | The `<img>` **is the detector**; without it nothing fires `onError` and a broken banner asserts its own health | — |
| Patch notes in the Manifest | **Included** — and the action paths are **guarded** | 2026-08-31 | The design lists them, and excluding them made the portal contradict its own export (*"37 of 37"* beside a strip saying 39). ⚠️ **Removing that gate also opened edit and remove on publications** — guarded in `4f4e211`, with `scripts/seasonPatchNoteGuard.test.js` proved able to fail | — |
| `.rowlife` | **Emitted with no rule, on purpose** | 2026-08-31 | The mockup emits it on every row and defines no rule either. **Removing it would change the class list the audit pairs on.** `portalOrphans` and `portalReverseOrphans` both know this via `scripts/lib/designClasses.cjs` | — |
| `.dend` · `.pill.ghost` | **Deleted** | 2026-08-31 | **Zero elements in either page.** Dead in the design too | — |
| The A/B artifact | **Dropped for Season** | 2026-08-31 | His call. §0.5b's published-frames exit condition is waived for this realm | — |

## Armory — realm-specific decisions

| Surface | Decision | Date | Why | Reopens if |
|---|---|---|---|---|
| `th.sortable`/`button.sortbtn` on Category, Gunsmith code, Attachments column headers | **Portal keeps sortable columns.** Class (b) | 2026-08-31 | The mockup renders these as plain static `<th>`; the portal made them clickable. Not part of the mode-collapse's stood-down redesign set (that queue is closed, Season-specific, and already done) — evaluated standalone. Harkirat: keep it | The sort feature is found to be broken or unused |
| `Manifest`'s `removeLabel` prop, Armory's call site | **Portal keeps `"Stage deletion"`.** Class (b) | 2026-08-31 | The mockup's generic `.sr` "Remove" label is misleading here: this realm's remove button stages a deletion, it does not delete — nothing is destructive until Review commits the changeset. `portal/ui/armory.js:1163` overrides the shared `Manifest` component's default `removeLabel='Remove'` on purpose | The staging model changes and removal becomes immediate |
| Armory masthead stats row (`armoryStats`, `weapons.size` replacing a "ranked" count) | **Portal keeps its own stat set.** Class (b) | 2026-08-31 | `portal/ui/armory.js:1003` documents the reason: the mockup's stats row counted `builds` twice — once as the hero figure, again three columns over — "two different measurements that happened to agree." The portal swapped the redundant stat for a distinct-weapon count, a considered fix | The masthead is redesigned again and the redundancy returns |
| `span.l`/`span.c` — Manifest's row-state legend vs Armory's data-quality legend | **Pairing artifact — different components, same class letters.** Not a decision, a diff-tool limitation | 2026-08-31 | `portal/ui/manifest.js:11`'s `PILL` map (`saved`/`staged`/`conflict`) is the shared Manifest's STAGING legend, present on every realm. The mockup's `.l`/`.c` are Armory's DATA-QUALITY legend (clean/needs-repair/stale) — unrelated concepts the audit matched only because they share single-letter classes | The audit tool's pairing algorithm is improved to consider semantic context, not just class names |
| `kbd.mh-k` — `New MP build` **B** / `New DMZ build` **D** vs the design's **N** and none | **Portal keeps `b`/`d`.** Class (b) | 2026-08-31 | `portal/ui/armory.js`'s `ADD_KEY` comment is the reason and predates this pass: the armoury has TWO create buttons and the design gives a shortcut to only one of them, so the second was unreachable from the keyboard. `b`/`d` are announced on their own chips rather than bound invisibly, and both are guarded so a bare letter typed into a field stays a letter | The masthead drops to one create button, or `n` is freed up |
| The Manifest's `mode` filter chip (`MP ×2` · `DMZ ×2` · `All ×2` in `--triggers`) | **Removed — it was a dead end, not a divergence.** FIXED | 2026-08-31 | The rows reaching the Manifest are already filtered to the active armoury, so every non-default value of that chip was guaranteed to render an empty table. It also duplicated the masthead's own mode switch, which is what put three `×2` entries in the trigger listing. `ARMORY_FILTERS` is now `[]` and the chipset carries Category only, which is what armory.html's chip row is | A realm hands the Manifest both armouries at once |
| `span.sp` — the design's rack line ends "· 125 MP builds live"; the portal's does not | **Portal drops the clause.** Class (b) | 2026-08-31 | The mockup ships a SAMPLE of the collection, so its meta line has to say how big the real one is. The portal reads the live collection, so the figure immediately to its left already IS that number — restating it prints the same count twice | The portal starts rendering a subset of the catalogue |
| `span.mh-take-n` — "133 builds · 4 formats" vs the design's "125 MP builds · 4 formats" | **Portal counts BOTH armouries.** Class (b) | 2026-08-31 | The design scopes its whole export strip to the active mode, so backing up DMZ means switching to it first. `armory.js`'s own comment already reserves the export strip as the one surface where both armouries are legitimately in scope. **The format count is no longer a difference** — the three narrow scopes the design has and the portal lacked (`This view`, `Category`, `First five`) were built this session, so both sides now say four | The strip stops offering a whole-catalogue scope per mode |
| Armory's `Category` export scope appears only once a chip is ON | **Portal diverges deliberately.** Class (b) | 2026-08-31 | armory.html keeps the row visible reading "pick one first" with a `build()` that returns the empty string, so its Download hands you a 0-byte file. A control that is present and lies is worse than one that arrives when it can act | `ExportBody` learns to render a disabled scope |
| `span.modetag` vs the mockup's `span.bc-mode` | **Portal keeps `.modetag`.** Class (b) | 2026-08-31 | `.bc-mode` has a rule in the PORTAL's sheet and none in the design's, while `.modetag` is defined in both — so the mockup emits a class its own stylesheet does not style. Same defect class as `.t-legendary` two rows up | The mockup package is regenerated and defines `.bc-mode` |
| `span.key.rkey` + `.rk-clean`/`.rk-bad`/`.rk-age` vs the design's two bare `.l`/`.c` spans | **Portal keeps the named legend.** Class (b) | 2026-08-31 | The design's legend reuses the generic row-state letters for a DATA-QUALITY vocabulary (clean · needs repair · stale), which is what put those two on the pairing-artifact row above. Naming them is what stops the two vocabularies colliding | The Manifest's staging legend is renamed and the collision goes away |
| `span.ok.thumb` ×125 in the Attachments cell, and the `.detcell` wrapper it needs | **Portal keeps them.** Class (b) — and they are most of the residual page height | 2026-08-31 | `.thumb` and `.detcell` are defined in the DESIGN's own stylesheet (6 and 5 rules); its Armory table simply does not emit them. Whether a build has an image is the single most common defect this realm exists to repair, and the row is where you are when you need to know | The Repairs view becomes the only place image presence is reported |
| `p.hint` "Click a row to open it." | **Portal keeps it.** Class (b) | 2026-08-31 | The rows open an editor on click and nothing else on the page says so. `armory.js`'s `.bed` comment records why it sits under the board rather than in a column of its own | The row-click affordance becomes visible some other way |
| `span.t` "Armory" in the shared view header | **Portal keeps it.** Class (b) | 2026-08-31 | season.html's `.ph` opens with `<span class="t">Season</span>`; armory.html's has no title at all. The two mockup pages disagree with each other, and the title is rendered by `shell.js` for all six realms | The mockups are regenerated consistently |
| Armory's Compare view diffs at −894px | **A DATA STATE, not a gap.** Not a decision | 2026-08-31 | The mockup pre-picks two builds into `st.cmp`; the portal starts with none, so it shows its empty-state paragraph where the mockup shows two cards and a field table. The portal renders every one of the mockup's Compare classes — `table.cmptab`, `tr.same`/`tr.diff`, `td.cmpf`/`td.dnow`, `div.dcard.lc`, `ul.lc-att`, `div.lc-code`, `div.lc-foot`. **`MAX_COMPARE` is 3 where the design's is 2**, which is strictly more capable and which the table and cards both handle | Someone compares the two views with the same builds picked on both sides |
| Armory's Add form and build editor render INLINE in `.bed`, not as drawers | **Portal keeps the inline panels.** Class (b) | 2026-08-31 | The editor's Discord preview lives beside the fields that produce it, which is what the adopted sheet's `.bed-side` split is for. ⚠️ **This also explains the prior pass's "inconclusive" `--open-sel` result**: clicking a table row REPLACES the rack with the editor, so a SHORTER page is the correct outcome, not an ambiguous one — there was no overlay to open | The preview moves back out of the editor |
| `p.hint` between the view panel and the Manifest panel | **Moved INSIDE the Manifest.** FIXED — and it was never the cosmetic row it was cited as | 2026-09-01 | 🔴 Both stylesheets carry `.panel + .panel{background:transparent}`, and the design's manifest IS the adjacent sibling of its view panel, so its table sits on `--desk` and reads as a well cut into the page. **One paragraph between the two panels broke that selector on the portal**, so every row of a 125-row table painted `--raised`: measured `#171E24` against the design's `#0F1418` at the same pixel. The identical rule, in both sheets, firing on one side. ⚠️ **Nothing could see it**: ④ reported it as one `section.panel backgroundColor` row among 149, the reverse-orphan sweep saw a rule WITH elements, and a class-signature diff sees no difference at all. Found by sampling the pixel | A realm needs an element between those two panels, at which point the rule needs a class rather than adjacency |
| `span.crumb` — the breadcrumb names the open view | **Portal keeps it, and it is AHEAD of the design.** Class (b) | 2026-09-01 | Measured with `--open`: mk “Armory” · pt “Armory Tier board”. ⚠️ **A deferred-list entry recorded this the other way round for two passes** and would have sent a session to remove the better behaviour. A breadcrumb that stops at the realm on a four-view realm tells you less than one that does not | The view name moves somewhere else in the chrome |
| `span.stg-clear.v` present in the design, absent in the portal | **A DATA STATE, not a difference.** Not a decision | 2026-09-01 | `shell.js` emits `stg-clear` when a STAGED figure is zero — the design dims a staged column at rest because that is good news rather than an absent number. The mockup fixture has 0 staged and the portal's has 1, so the class correctly does not apply. Surfaced by `portal:inventory`, which no other instrument had run on this realm | The two fixtures are aligned on staged count |
| `span.id` in the identity chip | **Cited with the raw-id row above.** Class (b) | 2026-09-01 | `shell.js:267` renders `…` plus the last four characters of the Discord id. It is the same PRIVACY question as the chip's text and is adjudicated with it, not separately | The privacy amendment lands |
| `col.c-state` matched nothing · `col.c-detail` emitted twice | **FIXED — the realm states its column width.** | 2026-09-01 | `manifest.js` derives a column class from `dataKind` when a realm supplies none, and `dataKind:'right'` fell to `c-detail` — so Armory's Badges column took the detail width and the design's `col.c-state` matched nothing. A column width is the DESIGN's call, so the realm now passes `col:'c-state'` rather than letting a default guess | The Manifest's default derivation is made design-aware, at which point the override is redundant |
| `td.code.drop-sm` in the design · `td.d.drop-sm` + `span.code` in the portal | **FIXED — `code` is its own cell kind now.** | 2026-09-01 | Armory's Gunsmith-code column declared `dataKind:'date'` purely to inherit `drop-sm`, so the cell rendered as the DATE cell in the mono data face. Visible where it reads worst: the *DMZ — no code* placeholder came out in JetBrains Mono against the design's Space Grotesk. **Borrowing a kind for its side effect is how a cell ends up lying about what it holds** | A sixth kind is needed and `code` is borrowed for it in turn |
| `portalAudit` and `portalDiff` report page heights 44px apart | **Different FRAMES, consistent under a transform.** Not a defect | 2026-09-01 | The audit walks the innermost `main`; the diff takes the largest content height across `main`, the scroll container and `document.body`. Both sides shift equally, so neither is wrong — but a record quoting one against the other invents a difference. **Cite one source per record and name it** | Either instrument changes its root |
| The realm's identity hue — the lead figure, the MP create chip's dot and the MP mode pill read **red** in the mockup (`--draw:#FF3430`) and **violet** in the portal (`#AE72E0`) | **Portal keeps its tokens.** Class (b) | 2026-09-01 | These are Season's topic accents, shared by all six realms, and they were settled in `tokens.css`'s own comment against the fixtures the Track renders from — changing them to match one realm's mockup moves every realm. ⚠️ **It is still the loudest single difference on the page**, the first thing the eye hits, and it makes the two read as different products. Recorded as a decision rather than a defect so it is not re-derived, but it is a product-identity question worth an answer | Harkirat picks a hue for the realm, at which point the tokens or the mockups move — not both |
| `span.ok.thumb` on all 125 rows, beside the BADGES column | **Portal keeps it.** Class (b) | 2026-09-01 | Supersedes and sharpens the earlier row: `.thumb` IS defined in the design's own stylesheet, and image presence is the defect this realm exists to repair. ⚠️ **Seeing it rendered, a green chip repeated 125 times competes with BADGES, which is the column that actually signals** — the affordance is right and its WEIGHT is arguable. Left as drawn rather than quietly restyled | The badge column stops being the row's primary signal, or the indicator is redesigned deliberately |
| The bulk-delete `Confirm`: `loadout.bulkDelete · tier 2` where the design shows `loadout.delete · tier 1`, and the title and `ul.dw-l` that follow | **Portal keeps the bulk op for a single row.** Class (b) — and it was already adjudicated in code before this pass looked | 2026-08-31 | `scripts/portalOpsReach.test.js:35` carries the equivalence and its reason as a CHECKED claim: *"the Manifest stages `loadout.bulkDelete` with a one-element id list, so one delete and many are one code path and one changeset row"*. The tier, the plural title and the named-builds list are all downstream of that one shape. ⚠️ **The diff makes this look like an over-gated destructive action and it is not a gate to lower on a diff's say-so** — CLAUDE.md's own rule: a FIX row that removes a gate is never a triage row | The Manifest gains a single-row delete path of its own, at which point `loadout.delete` needs its own reachability |

## Broadcast — realm-specific decisions

*Created empty 2026-09-01 by the Armory session. **An empty section is not the same as an unexamined realm** — this table exists so a Part 3 finding has somewhere to land and so `ctx_search` against this ledger returns a HEADING rather than nothing. "Returned nothing" would otherwise read as "never decided" when it means "never sectioned".*

| Surface | Decision | Date | Why | Reopens if |
|---|---|---|---|---|
| `th.sortable.ta-r` + `button.sortbtn` on the State column · `col.c-ra` · `button.sortbtn` mk×4 pt×5 | **Portal keeps them.** Class (b) | 2026-09-01 | Two separate inheritances from the shared `Manifest`, both already settled on Armory. The sortable header is that realm's own row applied to the same component. `col.c-ra` is the reverse: `broadcast.html:66` declared **five** `<col>`s for a table whose rows carry **six** cells — it calls `S.removeCell` and never widened its colgroup — so the portal emitting the sixth was the mockup's omission, not a divergence. 🔴 **RE-GRADED CITED → FIXED, 2026-09-04 20:16 EDT: the sixth `<col class="c-ra">` and its 40px rule are in the package now**, under §0.1a test 2. Measured after: broadcast holds at 17 regions with **② 12 → 10 and ④ 2 → 1** — the `colgroup width: 1120px → 1160px` ④ row is gone | The package sheet stops defining what its pages emit |
| `button.chip` "All" reported ONLY IN MOCKUP by ② SHAPE | **Pairing artifact — both sides have it.** Not a decision | 2026-09-01 | `portal:audit --triggers` lists `All · live · scheduled · expired` on **both** sides, 17 triggers each. The LCS alignment paired the chips against their neighbours, exactly as it did with Season's `All` chip and its two `Sep 15` nodes. ⚠️ Checked against the page before closing, per §0.10. Re-verified 2026-09-04 14:22 EDT: mk 17 · pt 17, both ONLY-IN lists empty | 🔴 **EITHER SIDE STOPS RENDERING THE `All` CHIP** — `npm run portal:audit -- --realm broadcast --triggers` no longer lists it on both. ⚠️ **This row's falsifier read *"the audit's pairing learns semantic context"* until 2026-09-04 20:17 EDT, and it could never fire:** the DECISION is a claim about the two PAGES and the falsifier was a change to the INSTRUMENT, so if either side actually dropped the chip nothing would reopen it — the row would keep asserting both sides have it while one did not. **A falsifier must name a property of the thing the decision is about.** |
| The unclassed `div` reported ONLY IN MOCKUP on both views | **A mechanism difference with no pixels.** Not a decision | 2026-09-01 | `broadcast.html:43-44` wraps each view in `<div id="viewQueue">` / `<div id="viewAir">` and toggles `hidden` to switch between them; Preact swaps children instead, so the portal needs no wrapper. The element carries no class and no rule, which is why it costs nothing in the diff | The portal starts needing a per-view container of its own |
| `p.expnone` — the design ties an export to unlocking one-way operations, the portal does not | **Portal keeps its wording.** Class (c) | 2026-09-01 | It is the same decision already kept in `oneway.js`: this console has no export interlock, and copy promising one would be a false claim about what the page enforces. One of the 13 kept portal decisions | The interlock is actually built |
| The two export scope NOTES (`broadcast.live`, `broadcast.all`) | **Portal keeps its notes.** Class (b) | 2026-09-01 | The file's own comment predates this pass: *"Each scope states its own shape, and neither is re-importable"* — the design's *"The full record"* does not say that a reader cannot feed the file back in, and for a realm whose whole artifact is free text that is the fact that matters. ⚠️ The scope **label** was NOT kept: the design's `What is live now` is adopted, because a scope names what is in the file rather than which tab you were on | A bulk-add format for announcements exists |
| `span.crumb` "Broadcast Delivery queue" vs the design's "Broadcast" | **Portal keeps the view in the crumb.** Class (b) | 2026-09-01 | Inherited from Armory's own finding, where the portal NOT naming the open view was the defect. Fixing it there made every realm's crumb name its view; the mockup pages never did | The crumb's contents are redesigned |
| `span.id` / `button.whobtn` rendering `…2283` | **Adjudicated PRIVACY question, not a conformance fix.** | 2026-09-01 | Same row as Season's and Armory's — `shell.js:267` prints the last four of the Discord id because nothing captures a username. It is `docs/db-deferred-list.md`'s `[P3 · S]` entry and needs a `PRIVACY.md` amendment, not an edit here. Also the whole of the `button.whobtn`/`span.who` `width: 86 → 100` ④ rows | The privacy amendment lands |
| `b.crumb-sep` ① CASCADE `+4 top / -6 height`, and the rail `svg`/`path`/`use` rows | **Portal keeps the inlined SVG.** Class (c) | 2026-09-01 | The standing cross-realm row, one table up. ⚠️ It is this realm's ① CASCADE line on every `--open`, and §0.7c's rule is explicit: **a CITED cascade is closed, not fixed** — work ④ through it | The text-glyph rule is retired |
| `button.pill.sm` `minHeight: auto → 0px` | **Portal keeps `min-height:0`.** Settled, not a divergence | 2026-09-01 | the `THE 32px FLOOR WAS MINE, NOT A DECISION` comment in `app.css` is the decision (`rg 'THE 32px FLOOR WAS MINE'` — it was cited as `:3886` and is at `:3892`) — *"THE 32px FLOOR WAS MINE, NOT A DECISION… Adopted the design's size"*. ⚠️ **Not `:3882`**, which sits inside the paragraph that SUPERSEDED it and says the opposite (*"NOT corrected to the mockup's 26px… Portal-ahead"*); a line number that lands on the retired half of a comment is R2 failing its second test, and `min-height:0` is what lets the design's size apply. The computed values differ; the rendered height does not | The control size is re-derived |
| The delete `Confirm`'s eyebrow, heading, body and button — `announcement.delete · tier 1` / `Remove this announcement?` / `Remove` in the design vs `tier 2` / `Stage deletion of 1 announcement?` / `Stage deletion` | **Portal keeps the staging language and tier 2.** Class (b) | 2026-09-01 | One decision in four rows, and Armory's `removeLabel` row already settled it for the shared component: this realm's remove button STAGES a deletion, it does not delete, and nothing is destructive until Review commits. The mockup itself carries both tiers — `broadcast.html:387` is tier 2 "Stage deletion" and `:502` is the tier-1 "Remove" this row control uses — so the design disagrees with itself and the portal follows the one that matches what the op actually does | The staging model changes and removal becomes immediate |
| `bulkActions` on Broadcast's Manifest — `Export selection` and `Stage deletion` | **REMOVED — unreachable, not a divergence.** FIXED | 2026-09-01 | Both render only in the bulk bar; the bulk bar needs a selection; `selectable={false}` is a deliberate conformance decision for this realm (its design draws no checkbox column and forcing one cost 38px of table width and wrapped a row). So two verbs were declared behind a control the realm had itself removed. Same class as Armory's Manifest `mode` chip. ⚠️ **Found only by `--open-sel button.rmv`** — `--triggers` filters data rows, so no listing could ever have shown it | The design gains a checkbox column for this realm |
| `.stt.saved` ink · the `PILL` state map · `td.nums` vs `td.d` · `span.ncell` | **FIXED, not kept.** | 2026-09-01 | See commit `804fa8b` and its message — ⚠️ **NOT the changelog**, whose newest entry is Armory's `v3.70.0`; this release's paragraph is the pre-merge checkpoint and cannot be written until the PR number exists. Recorded here only so a later run that meets these again knows they were work rather than decisions | **A record, not a decision — it correctly has no falsifier**, because nothing reopens a defect that is gone |

## Access — realm-specific decisions

*Created 2026-09-01 by the Access session. **An empty section is not the same as an unexamined realm** — the same convention the Broadcast section records: a Part 4 finding needs somewhere to land, and `ctx_search` against this file must return a HEADING rather than nothing, because "returned nothing" reads as "never decided" when it means "never sectioned".*

| Surface | Decision | Date | Why | Reopens if |
|---|---|---|---|---|
| `mxrole` · `mxrow` | **Dead on both sides.** Not a decision | 2026-09-01 | Measured: 8 rules in the design's `assets/app.css`, 8 in `portal/ui/app.css`, **zero emissions** in `access.html`, the package's own `docs/superpowers/mockups/2026-08-23-portal-interactive/assets/shell.js` or `portal/ui/access.js`. Identical disposition to Broadcast's `atbar`/`atrow`/`timax` group. ⚠️ `portal:reverse-orphans` cannot answer this question — its scope is `portal/ui` emitters and rules and it never reads a mockup page, so it sees only the portal half | Either side starts emitting one |
| The grant composer renders **inline** at the foot of the grid; the design opens a **drawer** | **Portal keeps the inline form.** Class (b) | 2026-09-01 | His call, batched with the Sessions fork at the start of the pass. Armory already set and kept this precedent for its Add form and build editor. The overlay system here is in use for confirms, so converting is a real rewrite for no stated gain, and the form and the grid it adds to stay on one screen. **Downstream: the design's `button.chip.go` "+ Grant access" in the grid foot stays unbuilt** — it would point at a form 20px below it — which is also the whole of the `div.mxfoot height: 57 → 72` row and the `span.sp` ABSENT row beside it | ⚠️ **The first falsifier here was *"the composer moves off the page"* — the negation of the decision itself, so no OBSERVATION could ever reopen it, and three further rows are declared downstream of this one. A falsifier that can only fire after somebody has already changed the thing is dogma with a column heading.** It reopens if the inline form pushes the grid's last row below the fold at 1282×888, or if a second grant entry point appears anywhere in the portal |
| Sessions | **FIXED — it was a third view tab and is now the design's second panel.** | 2026-09-01 | His call. `access.js`'s own comment argued the shared `Manifest` was the wrong home for session rows — which is an argument against `Manifest`, never one for a tab. The bespoke `Sessions` component moved into `Shell`'s existing `footSlot` unchanged, and the view bar drops to the design's two tabs. **A record, not a decision — correctly no falsifier** | — |
| `th` "Action" where the design's is `&nbsp;` | **Portal keeps the label.** Class (b) | 2026-09-01 | A `<th>` holding a non-breaking space is an unnamed column for a screen reader walking the table header. Naming the column costs nothing and the design's blank cell is an omission rather than a choice — same class as `.bc-mode`, a mockup artifact rather than a design decision | ⚠️ **The falsifier was "the actions column stops being a column", which tests nothing about the ACCESSIBILITY this row justifies itself on.** It reopens if a screen-reader pass shows the design's `&nbsp;` header is announced acceptably, or if the Manifest gains a convention for unlabelled action columns that this should follow |
| `button.chip` "Edit" on every grid row — the `80x176` region at `(1152,496)` | **Portal keeps the inline grid edit.** Class (b), and the note gap is FILED | 2026-09-01 | The design offers BOTH a click-to-toggle grid and an Edit drawer; the portal offers the grid, which edits the same permissions through `/api/access/grant` — the endpoint replaces the whole list either way. **What the drawer also edits and the grid cannot is the admin's NOTE**, and that is a real gap: filed in `docs/db-deferred-list.md` rather than built, because a new editor with its own fields and confirm is a feature, not a conformance fix | The note becomes editable, at which point this row is the design's |
| `span.v.zero` ABSENT · `div.estate` ABSENT · `span.rt` "none" vs "1 active · 2 total" · `span.mxs.mxcol` "MP" vs "CALENDAR" | **DATA STATES, not differences.** Not decisions | 2026-09-01 | The mockup fixture has **0** sessions and the harness has 2, so the design draws its empty state and a zero-valued stat where the portal draws a list. Same class as Armory's `span.stg-clear.v` row. ⚠️ Two of them are also **coverage limits of the real-server pass** — see the dev-data note below | The two fixtures are aligned on session count |
| `span.sp` `fontSize: 11.5px → 10.5px` | **Pairing artifact of a deliberately-unbuilt element.** Not a decision | 2026-09-01 | `.ph .sp{font-size:10.5px}` is byte-identical in both stylesheets. The 11.5px element is the design's **grid-foot** `span.sp` — the flex spacer beside the "+ Grant access" chip, which the composer decision leaves unbuilt — so converge pairs it against the portal's new sessions-header `.sp`. Two different `.sp`s, one of which does not exist here on purpose | The grid-foot chip is built |
| The grid's `.mxgrp` group headers | **Both sides DERIVE the colspans. No defect** | 2026-09-01 | 🔴 **Part 4's prompt warned that "the COLUMNS come from `accessScopes` order" so appending a command silently mis-groups the grid, and that the overlay could not see it. It does not reproduce on either side.** `access.js:164-165` spans `commands.length` / `pages.length` off a `kind` filter, and `access.html:153-154` spans `COMMANDS.length` / `PAGES.length`. Checked on the real server, which is where the prompt said it would show | Either side hardcodes a span |
| `span.id` `…2283` vs the design's `dior` · `b.crumb-sep` ① CASCADE · `button.pill.sm` `minHeight` | **Cited to the cross-realm and Broadcast rows.** Class (b) | 2026-09-01 | The privacy adjudication, the inlined-SVG rule and the `min-height:0` decision, all settled on earlier realms and all reported on every Access run | Their own rows reopen |
| `p.racknote` `h 83 → 66` — the design's paragraph says one clause twice | **Portal drops the repeat.** Class (c) | 2026-09-01 | `access.html:230-238` closes with *"because a convenience that quietly hands out irreversibility is not one"* — the same clause it already used four lines earlier in the same paragraph. Restoring it buys 17px of height by shipping a sentence that says the same thing twice, which is the copy defect the UX-copy audit's whole §F is about. **The whole of the 17px, and the small `code`/`b` offsets below it** | ⚠️ **"The design's racknote is rewritten" was unfalsifiable in practice — nobody rewrites the mockup.** It reopens if a reader misses the `all`-never-includes-`destructive` rule in testing, or if the dropped clause turns out to be the only statement of it on any surface |
| `.klg.spofk` — 3 rules a side, now with no emitter | **Dead on both sides.** Baselined, not deleted | 2026-09-01 | The portal's By-permission list carried a `span.klg.spofk` legend; it moved into the view bar with the other marks, because the mark appears on BOTH views and two copies are two authorities for one mark. **The design never emitted `.klg` either** — `access.html:38` draws its key as `span.key` inside the `.ph`. Deleting the portal's three rules would make the two stylesheets disagree where they currently agree, which is the `.rowlife` precedent read the other way round | Either side starts emitting it |
| `.lone` emitted with no rule | **Matching the design, which does the same.** Not a defect | 2026-09-01 | `access.html:257` computes `class="scope ${flag}"` where flag is `spof` or `lone`, and NEITHER stylesheet defines `.lone` — the visible mark is the `span.flag.quiet` inside the row, and `.lone` is a hook. ⚠️ `portalReverseOrphans` filed it under ④ rather than under its own 📐 "inherited from the design" heading, because the design emits it from an unresolvable expression too; `.rowlife` gets the 📐 treatment only because season.html emits it as a literal. **The scanner's category is about what it can PARSE, not about what is true** | Either stylesheet gains a `.lone` rule |
| The masthead sub, and UX-copy **F1**'s "one rhetorical template used three times" | **Out of scope until the redesign phase.** Not a decision yet | 2026-09-01 | Both sides render the sub identically, so flattening it would move the portal AWAY from the design mid-conformance. `CLAUDE.md`: redesigns wait for all six realms — re-affirmed 2026-08-31 against my own argument | The redesign phase begins |

**⚠️ Dev-data reachability — a clean real-server walk over an empty state space is not evidence.** Counted against `.env.dev`'s localhost Mongo on 2026-09-01 18:20 EDT: **3 admins, 8 portal sessions**, exactly **one** holding a bare `manage` — so the `inh inherited` cells are reachable, `locked` is reachable now that the owner row is synthesized, `spof` and `lone` are both reachable, and `pend`/`pend off` are reachable only by clicking. **Two states the realm draws are NOT reachable in dev and the walk's silence about them means nothing**: the sessions empty state (`div.estate`, needs 0 sessions) and the no-admins empty state (`p.empty`, needs 0 admins). Seeding is out of scope; naming the gap is not.

## Analytics — realm-specific decisions

*Filled 2026-09-04 21:40 EDT by folding in `local/difference-ledger-analytics.md`'s 33 rows VERBATIM — the wording is Part 5's own, not a rewrite. 🔴 **THE WORK WAS NEVER MISSING; THE RECORD WAS.** Part 5 ran to enumeration on 2026-09-01 (57 of 62 converge mismatches grouped into seven, every printed diff region mapped to a row), and every one of those rows lived in a GITIGNORED per-realm file — so a worktree, a fresh clone, and every `ctx_search` against the tracked ledger saw a realm with **zero** decisions sitting at 14.2%. ⚠️ **Five other per-realm ledgers are in the same place** (358 lines across six files under `local/`); Analytics is simply the one whose tracked side was empty, which is why the absence was visible at all. Originally: created empty 2026-09-04 20:17 EDT. 🔴 **ANALYTICS WAS THE ONE REALM WITH NO SECTION AT ALL** — at 14.1% and 41 regions, the second-largest resting difference in the portal, with zero recorded decisions. It was found by `scripts/portalLedgerRows.mjs` on its first run, and only because that tool DECLARES the seven realms: counting every `##` heading returned six realm names and three non-realm ones, and an absent realm is indistinguishable from a section that is merely not a realm. **An empty section is not the same as an unexamined realm** — this table exists so an Analytics finding has somewhere to land, and so `ctx_search` returns a HEADING rather than nothing. "Returned nothing" would otherwise read as "never decided" when it means "never sectioned".*

| Surface | Decision | Date | Why | Reopens if |
|---|---|---|---|---|
| `div.hsplit` region on Health — mockup draws "Where the milliseconds go", portal draws Last boot / Alerts by level / Restarts / Where these come from (page-diff region 1, 1168x1440) | **Portal keeps it.** | 2026-09-01 21:36 EDT | Harkirat chose to keep the dependency panel on **Timing**, where the view's subject is duration and where the portal's version is richer (per-call-average colour rather than share-of-total) · pop-up answer, 2026-09-01 21:36 EDT; portal reasoning at `analytics.js:433` | He asks for the design's version |
| `div.depb` x4 · `div.depb.slow` x2 · `div.depbars` · `span.dn/dt/dv/dc` x6 · `col.c-win` · `td.nums` x12 · `td.drop-sm.ta-r` x12 · `th.drop-sm.ta-r` · `span.rowmeta` x12 · `col.c-state` — all ONLY IN MOCKUP | **Portal keeps it.** | 2026-09-01 | Every one is inside the panel in row 1. They are one decision, not eleven · same as row 1 | — |
| `div.hbanner.warn` mockup-only vs `div.hbanner` portal-only, `span.hbi` orange vs green | **Portal keeps it.** | 2026-09-01 22:05 EDT | Two different banners in the same place, downstream of row 1: the mockup's Health banner belongs to the dependency panel; the portal's is its own "these are the bot's records, not a live reading" note. The mockup's banner now also exists on the portal's **Timing** view, where the panel lives · 2026-09-01 22:05 EDT, this pass | — |
| River table: 5 columns (When · Kind · Source · What · Who) against the design's 4 | **Portal keeps it.** | 2026-09-01 21:36 EDT | Harkirat chose to keep the Source column. The river merges three collections onto one timeline and Source is the only thing saying which one a row came from — `analytics.js:611` states that in the export note · pop-up answer, 2026-09-01 21:36 EDT; `portal/fixtures/geometry/analytics.json` already recorded the five | He asks for the design's version |
| Usage's fourth column is **share**, the design's is duration | **Portal keeps it.** | 2026-09-01 21:36 EDT | Harkirat left it unticked. Timing already owns duration; a duration column on Usage duplicates it · pop-up answer, 2026-09-01 21:36 EDT | He asks for the design's version |
| Export scope set — five a side, two shared ids, `span.mh-take-n` "32 records · 5 formats" vs "CSV for a spreadsheet · 5 tables" | **Portal keeps it.** | 2026-09-01 21:36 EDT | Harkirat left it unticked. The five scopes are written as source literals because `portalExport.test.js` reads them that way (`analytics.js:605`), and the strip's own summary shape is a cross-realm decision — the ledger already carries the identical Armory row · pop-up answer, 2026-09-01 21:36 EDT; `docs/reference/portal-decision-ledger.md` Armory `span.mh-take-n` row | He asks for the design's version |
| Masthead: `commands 24h` (lead) · `errors 24h` · `uptime` · `worst ack` against `interactions` · `succeeded` · `errors` · `worst ack` | **Portal keeps it.** | 2026-09-01 21:36 EDT | Harkirat asked for "a hybrid of the most useful pieces of information that would actually be useful at a glance to me", so three of the four labels and their values are deliberately the portal's. The SHAPE now matches: four stats, lead first, error toned, worst-ack last · pop-up answer, 2026-09-01 21:36 EDT | He asks for the design's version |
| `div.lvlb` x14 portal-only | **Portal keeps it.** | 2026-09-01 | `DailyBars` — two panels the design has nowhere, reusing the `.lvlbars` markup. Portal-ahead, cited in place at `analytics.js:79` · pre-existing decision, comment dated in file | — |
| `div.panel` x1 portal-only | **Portal keeps it.** | 2026-09-01 | The DailyBars panel's own wrapper, same decision as row 8 · same as row 8 | — |
| `button.sortbtn` x5 · `th.sortable` x5 · `span.cb` x12 · `col.c-cb` · `col.c-detail` x3 · `span.dot` x11 · `span.id` | **Portal keeps it.** | 2026-09-01 | All inherited from the shared `Manifest`, settled on Armory and re-settled on Broadcast. Not this realm's work · `docs/reference/portal-decision-ledger.md`, Broadcast section | — |
| Usage row keys unprefixed (`gunsmiths`) against the design's `/gunsmiths` | **Portal keeps it.** | 2026-09-01 22:05 EDT | **The portal is right and the design is wrong about its own data.** The busiest names in that collection are background jobs; prefixing them asserts that `/webp_nameplate` is something a person can type, and nothing else on the screen corrects that · `analytics.js:319`, comment predates this pass; re-confirmed 2026-09-01 22:05 EDT | — |
| `span.srch` / `label.sr` "Search events" against "Search alerts and changes" | **Portal keeps it.** | 2026-09-01 22:05 EDT | The river holds alerts, changes AND restarts. The design's label names two of the three; the portal's covers all of them and agrees with `rowNoun=['event','events']` · 2026-09-01 22:05 EDT, this pass | — |
| `span.mlabel` "One history, both front doors" against "Manifest" | **Portal keeps it.** | 2026-09-01 | The portal titles the panel with what it is FOR; "Manifest" is the component's name, not the reader's word for it. Cross-realm — every realm titles its Manifest this way · pre-existing, cross-realm | — |
| `div.bvnote` on Search — the design names `zeroResults` in a code chip and cites a live example row; the portal writes the same point without the field name | **Portal keeps it.** | 2026-09-01 22:05 EDT | Kept as the portal's. The field name is not something a reader can act on, and this file already carries Harkirat's ruling against a page fluent in a dialect its sole reader does not speak (`analytics.js:17`). ⚠️ The design's concrete example ("somebody typed X into /manage's action field") IS stronger writing, and porting it is a real improvement that was NOT taken here — it needs the term data and trips `portalUi.test.js`'s chip-before-punctuation gate · 2026-09-01 22:05 EDT, this pass — decided, not overlooked | He asks for the design's version |
| `span.rt` "11 of 11" against "12 shown · 1323 recorded" | ☐ **OPEN — flagged, not decided.** | 2026-09-01 22:05 EDT | Shared `Manifest` count line, cross-realm. ⚠️ **NOT adjudicated this pass** — the design's phrasing is more informative and this is a candidate for the row 7 sweep · open, flagged 2026-09-01 22:05 EDT | It is decided |
| `minHeight: auto -> 0px` on `button.pill.sm` · `section.hpanel` · `span.tl-*` · `span.info.sev` | **Portal keeps it.** | 2026-08-31 | Cross-realm control tokens, already settled app-wide. None carries a paired dimension change except `section.hpanel`, which is the cited full-width Timing panel · `docs/reference/portal-decision-ledger.md:41` — adopted 2026-08-31, falsifier is "a realm shows button metrics that look wrong" | — |
| `⑤ RULES 2011 / 41 / 40`, byte-identical on all five views | **Portal keeps it.** | 2026-09-01 21:26 EDT | Cross-realm by construction — identical across every view means it cannot be this realm's work · five `--all` audits, 2026-09-01 21:26 EDT and again 22:0x EDT | — |
| `span.boot.rivk` mockup-only · `.ub2`/`.durrow`/per-command DIFFERENT-COUNT rows | **Not a decision — an artifact.** | 2026-09-01 | Fixture shape, not design. `foldByCommand()` groups by command exactly as `computeUsageStats` does, where the mockup's `cmds()` lists command+subcommand; "fixing" it reintroduces the >100%-share defect `stub.js` records · `portal/ui/harness/stub.js:127` | — |
| `th` "When" vs "" and `b.ok` "connected" vs "59 synced" | **Not a decision — an artifact.** | 2026-09-01 22:05 EDT | LCS pairing artefacts — both strings exist on both sides, aligned against different neighbours because the portal's table carries a checkbox column · 2026-09-01 22:05 EDT, checked against the running pages | — |
| The four Health TILES carry different subjects — portal `Restarts 7d · RAM at last alert · Distinct users 24h · Quiet alerts 24h` against the design's `Interactions · Success rate · Restarts · Memory` | **Portal keeps it.** | 2026-09-01 22:42 EDT | **Portal keeps its set.** `analytics.js:138` records why: the design's tiles repeat three figures the masthead has just stated, and each portal tile carries something the masthead cannot. Accounts for diff regions 7 and 9 · 2026-09-01 22:42 EDT | — |
| Filter chips 7 (mockup) against 9 (portal), and `ABSENT mk button.chip ×4` in converge | **Portal keeps it.** | 2026-09-01 22:42 EDT | The portal filters by kind AND by level; the design offers kind plus a level chip row built from the data. Same capability, different grouping, and the level set is the four the writer can emit · 2026-09-01 22:42 EDT | — |
| `div.mh-anchor` mockup-only, height 0 | **Not a decision — an artifact.** | 2026-09-01 22:42 EDT | The mockup's export strip anchors to an empty div; the portal's strip is a Shell surface with no anchor. A mechanism difference with no pixels · 2026-09-01 22:42 EDT | — |
| `div.panel` + a second `div.ph` per view, portal-only | ☐ **OPEN — flagged, not decided.** | 2026-09-01 22:42 EDT | Every Analytics view wraps itself in its own panel inside the Shell's view panel. ⚠️ **NOT collapsed** — it is this realm's composition on all five views and unwinding it is a layout refactor, not a conformance edit. The DUPLICATED HEADER LINE inside it was the real defect and is closed (row 27) · 2026-09-01 22:42 EDT | It is decided |
| `ABSENT mk span.sp` — the design's per-view meta line | ✅ **Closed — it was a gap, not a difference.** | 2026-09-01 22:42 EDT | **CLOSED 2026-09-01 22:42 EDT** — was a gap. `Shell` has carried a `meta` slot since Broadcast hand-rolled one; access, armory, season and broadcast all use it and Analytics was the only realm that did not. The five lines were MOVED there from the nested panel headers, not added, so the sentence is not drawn twice | — |
| River rows open an event drawer | ✅ **Closed — it was a gap, not a difference.** | 2026-09-01 22:28 EDT | **CLOSED 2026-09-01 22:28 EDT** — was a gap, not a difference. The design opens one from every row (`analytics.html:545`) and `--triggers` structurally cannot list a handler bound to a `tr`, so no instrument reported it. Found by the completeness sweep's scope-conservation pass against §L row 5's own wording | — |
| `Manifest` rows were mouse-only wherever `onRowClick` is passed | ✅ **Closed — it was a gap, not a difference.** | 2026-09-01 22:28 EDT | **CLOSED 2026-09-01 22:28 EDT, shared surface** — `role=button` + `tabindex` + Enter/Space. Affected Armory too, which was reachable only through a rename input that happens to sit in its rows. The design states the reasoning at `analytics.html:540` | — |
| Pressing a level row shows an empty river in the harness | **Portal keeps it.** | 2026-09-01 21:52 EDT | Correct on BOTH sides and not a defect: the harness river is a five-row sample and the mockup's own level filter does the same thing to the same rows. On the real server the river holds every alert · 2026-09-01 21:52 EDT, verified by clicking on the running page | — |
| `div.hsplit` 1168x1440 — milliseconds panel against Restarts | **Portal keeps it.** | 2026-09-01 | row 1 | — |
| `td.nums` against `span.lp` | **Portal keeps it.** | 2026-09-01 | rows 1, 8 | — |
| `span.v` 491 against 23 | **Portal keeps it.** | 2026-09-01 | row 7 | — |
| `span.dc` against `div.bootgrid` | **Portal keeps it.** | 2026-09-01 | rows 1, 3 | — |
| `p.hp` against `div.bootcard` | **Portal keeps it.** | 2026-09-01 | rows 1, 3 | — |
| `input.cb-in` on BOTH sides | **Not a decision — an artifact.** | 2026-09-01 | pairing artefact — the command-bar input, same element both sides | — |

## Review — realm-specific decisions

**Reproduce:** `node scripts/portalDiff.mjs --realm review --portal harness --mk-query demo=1` · `npm run portal:audit -- --realm review --all --mk-query demo=1`. ⚠️ **BOTH COMMANDS SAID `--realm home` UNTIL 2026-09-04 14:16 EDT**, under the Review heading, and the paragraph beneath them quotes Home's page heights (1243/1199) while this section's own table says mk 888 · pt 984. This file is QUERIED rather than read start to finish, so a wrong `Reproduce` line returns another realm's answer to a Review question. Found by the §L ⑥ document agent.

*Added 2026-09-02 23:37 EDT, Part 6a. Every row here is a difference that REMAINS in the resting overlay at 0.5% and is adjudicated, not open.*

✅ **RE-ENUMERATED TO TWELVE, 2026-09-04 20:41 EDT — and the movement is the badge defect closing.** `node scripts/portalDiff.mjs --realm review --portal harness --mk-query demo=1` → **0.6% of pixels differ, in 12 region(s). mk 888px · pt 984px.** Regions 7 and 8 of the thirteen were `a.realm.out` reading `REVIEW 4` in the mockup and `REVIEW` in the portal; the portal passes `badges` now, both sides read `REVIEW 4`, and the two regions collapse to one. **Every one of the twelve is accounted for:**

| # | Region | What it is | State |
|---|---|---|---|
| 1 | 256×48 (992,0) `span` "staged · review" | The header commit chip — the two sides draw it differently | 🔴 **HARKIRAT'S CALL**, put to him 2026-09-04 20:41 EDT with both captures: `https://claude.ai/code/artifact/6fd1be3f-d276-4b09-b6af-a16440ffecae` |
| 2 | 240×48 (368,0) `input.cb-in` | The command bar's input, EMPTY on both sides | Cited — sub-pixel |
| 3 | 288×16 (960,192) `span.sp` | "commits as one transaction" vs "4 transactions, committed in order" | Cited — its own row below |
| 4 | 96×48 (832,0) `div.cmdbar` "⌘K" | Shared chrome | Cited — the crumb/chrome class |
| 5 | 96×16 (208,16) | mk `span` "Review" against the whole pt `header` | **Pairing artifact** — the whole-page node, the standing ② subtraction |
| 6 · 8 · 9 · 10 · 11 · 12 | `svg` / `path` / `use` at (16,64) (32,128) (32,192) (32,256) (32,320) (352,688) | The rail and gate icons | Cited — the settled sprite-versus-inline class (c) |
| 7 | 16×48 (0,400) `a.realm.out` "REVIEW 4" | **Both sides now read `REVIEW 4`** | The badge row below, FIXED |

🔴 **AND FOUR CITED ROWS OF THE ORIGINAL TWELVE NO LONGER REPRODUCE — the case that had no rule until 2026-09-04 20:41 EDT.** Rows 6, 7, 10 and 11 of the 2026-09-02 enumeration were `span` sub-lines at (256,256) (288,320) (240,416) (208,480); none appears in any run since. Under §0.7c's new FIFTH bucket a citation whose difference is gone returns to unadjudicated, so they were re-decided rather than left asserting themselves: **the sub-lines they described are no longer drawn differently, and the rows are recorded closed rather than deleted** — the record of why they were cited is what stops the same argument a third time. ⚠️ **The percentage did not move while this happened** (0.5% then, 0.6% now), which is exactly the reading §0.7d retired: a session re-running this and seeing "0.5%, matches" would have stopped.

| Surface | Decision | Date | Why | Reopens if |
|---|---|---|---|---|
| `a.realm.out` — the rail's staged count (was regions 7 · 8, now one) | ✅ **FIXED 2026-09-04 20:41 EDT.** All seven realms pass `badges`; both sides read `REVIEW 4` and Review's enumeration fell 13 → 12 | 2026-09-04 14:18 EDT · fixed 2026-09-04 20:41 EDT | ⚠️ **THIS ROW SAID "`badges` has no producer anywhere… the badge has never rendered once" UNTIL 2026-09-04 16:37 EDT, AND THAT WAS FALSE.** **`badges` is produced on TWO realms and five pass nothing.** `home.js:302` sends `badges=${{ review: staged }}` and `season.js:1248` sends `badges=${{ review: stagedCount }}`; `armory`, `broadcast`, `access`, `analytics` and `review` pass none, so the rail's staged count is absent on **5 of 7** realms. `rg -n 'badges=' portal/ui/` — so the badge renders on Home and Season and is absent on the other five, which is why it is absent on REVIEW, the realm whose own diff this row explains. `Rail` and `Shell` do declare `badges = {}` and default it; that is the shape, and the conclusion drawn from it was inverted by reading Armory's unrelated build-badge tokens as the search result. Found by the reader-review agent. `shell.js:47`'s own comment says *"THE PORTAL STAGED FROM SIX REALMS AND ACKNOWLEDGED IT NOWHERE BUT A TOAST… This is that function"*, directly above code that cannot do it: the same class as `oneway.js`'s seven ops with no button, and the same class [[feedback_comment_is_a_receipt]] names. The two sides' MARKUP is identical, which is why every structural gate is silent — this is a prop with no data | A realm stops passing `badges` — `rg -c 'badges=' portal/ui/` returns fewer than 8 (seven realms plus the Rail's own default) |


| Surface | Decision | Date | Why | Reopens if |
|---|---|---|---|---|
| `span.sp` — the changeset meta line | **Portal keeps "N transactions, committed in order".** Class (b) | 2026-09-02 23:37 EDT | The design says "commits as one transaction". `review.js`'s commit loop posts each changeset separately and `commitSet` wraps ONE changeset in one transaction, so with four staged changesets the portal's sentence is true and the design's promises an atomicity the product does not provide. The design models the staged set as a single changeset; the product mints one per staging action. ⚠️ On the screen whose whole subject is atomicity, the weaker true sentence beats the stronger false one | The product ever commits a whole board in one transaction |
| `button.rvdrop` — `✕` vs an inlined sprite icon | **Portal keeps the icon**, and keeps its `aria-label` | 2026-09-02 23:37 EDT | `reference_never_text_glyphs_for_icons`, same class as the crumb separator. The design's `✕` is a text glyph with no accessible name; the portal's control is named "Discard &lt;op&gt;" | The standing icon rule is retired |
| `.rvgate svg` — the gate icon's SIZE | **Design's 14px, and this was a real defect** | 2026-09-02 23:37 EDT | `.ic.sm` is two classes; `.rvgate svg` is one class and a type, so the shared icon component's modifier WON on specificity and the icon drew at 9.77px with `flex:none`. Fixed by giving the rule `.rvgate svg,.rvgate svg.ic`. ⚠️ Specificity, not order — moving the rule later in the file would have changed nothing | An icon in a gate line measures anything but 14px |
| `button.chip.go#seed` — *Load a sample changeset* | **Permanently ONLY-IN-MOCKUP** | 2026-09-02 23:37 EDT | A demo affordance the mockup's own comment says must not ship (`review.html:253-258`, COMPANION:2381). `portalInventory` will report it forever | The mockup drops the button |
| The tier-3 surface at rest — export gate, typed confirm, red chip | **NEITHER side may fabricate it.** Class (b) | 2026-09-02 23:37 EDT | `shell.js`'s Store derives an op's tier from `FIX.OP_TIERS` and only ever CHECKS a claimed one, because the tier is a property of the OP — `draw.delete` is tier 1 since its inverse is exact. `sampleOps` holds no genuinely tier-3 op, so the mockup CANNOT render the gate, and the harness used to type one on, teaching a state production's `/api/review` can never produce. The behaviour is proven instead by `npm run portal:reviewwalk` (assertions 6a–6f) against the real server | A genuinely tier-3 op joins `sampleOps`, or a states fixture photographs the surface |
| The board at rest | **Compared SEEDED, never empty.** `?demo=1` + `--mk-query` | 2026-09-02 23:37 EDT | The staged-ops store is `sessionStorage` and every instrument clears it on load, while the harness carries four changesets — so an unseeded run compares an empty page with a populated one and returns well-formed numbers for a comparison nobody meant. 4.7% / 15 regions unseeded against 0.5% / 12 seeded. "Seeded on request, never automatically" is COMPANION §15's own rule | The mockup's empty state is what is being compared, deliberately |

### Review — the twelve resting regions, mapped onto the four classes

*Added 2026-09-03 09:07 EDT because §L's close condition ② closes on the ENUMERATION, and until now that enumeration lived only in `local/difference-ledger-review.md`, which is gitignored — so a reviewer, a fresh clone and every future session could see the claim and not the evidence. Reproduce with `node scripts/portalDiff.mjs --realm review --portal harness --mk-query demo=1`; the instruments refuse without the seed.*

**0.5% of pixels differ, in 12 regions. mk 888px · pt 984px.**

| # | Region | Class |
|---|---|---|
| 1 | 224×48 at (1024,0) — `span` vs `header` | cross-realm — the account chip |
| 2 | 272×48 at (416,0) — `input.cb-in` both sides | cross-realm — the command bar |
| 3 | 288×16 at (960,192) — `span.sp` | portal is right — the atomicity sentence |
| 4 | 80×48 at (880,0) — `div.cmdbar` vs `kbd` | cross-realm — the ⌘K hint |
| 5 | 96×16 at (208,16) — `span` vs `header` | cross-realm — the crumb |
| 6 | 48×32 at (256,256) — `span` sub-line | mockup defect, repaired |
| 7 | 48×32 at (288,320) — `span` sub-line | mockup defect, repaired |
| 8 | 32×32 at (16,64) — `svg` | icon system — portal keeps the sprite |
| 9 | 16×48 at (0,400) — `a.realm.out` | icon system |
| 10 | 48×16 at (240,416) — `span` sub-line | mockup defect, repaired |
| 11 | 48×16 at (208,480) — `span` sub-line | mockup defect, repaired |
| 12 | 16×32 at (32,128) — `svg` | icon system |

### Review — the overlay tier, the only one measured

*The resting page is not the whole realm. `--open "Discard all"` opens on BOTH sides — the first overlay ever compared on any realm but Season — and reports **4.0% in 6 regions AT FOLD HEIGHT**, comparable only to a `--fold` run and never to the 0.5% full-page figure. Four regions are `div.scrim.on` at the page corners. The two real ones are **`aside.drawer.open` at 269.25px against 243.34px** (with the matching transform) and **a `span.dw-eye` eyebrow the design does not draw**. Both live in `portal/ui/overlay.js`, which every realm shares, so the fix is a shared-chrome pass and is filed rather than done. `--open "Commit 4 changes"` REFUSES: nothing opens on the mockup side.*

## Home — realm-specific decisions

*Added 2026-09-03 21:38 EDT, Part 6b. Every row here is a difference that REMAINS in the resting overlay at **0.7% / 27 regions, both pages 1243px** (this said 26 until 2026-09-04 14:16 EDT, the fourth carrier of a figure §L had already declared stale), measured SEEDED (`--mk-query demo=1`). Adjudicated, not open.*

| Surface | Decision | Date | Why | Reopens if |
|---|---|---|---|---|
| The unclassed `div` wrapping the staged strip | **Mechanism, not design.** Not a decision | 2026-09-03 21:38 EDT | `index.html` declares `<div id="resume">` and fills it with `innerHTML`; Preact renders `.hres` directly into `.home`. Same shape as Broadcast's `#viewQueue` wrapper, already carried there | The design gives the wrapper a rule of its own |
| `button.chip` — *Discard all* | **Portal omits it.** Class (b) | 2026-09-03 21:38 EDT | `home.js`'s `Resume` states the reason and it predates this pass: Review is the only screen that commits, and discarding everything from a summary strip — with no list of what is about to go — is the one shape of that button nobody should press. The count and the route are what this strip can honestly carry | A discard affordance grows a preview of what it would drop |
| `button.chip.go` vs `a.chip.go` — *Review & commit* | **Portal keeps the anchor.** Class (c) | 2026-09-03 21:38 EDT | It navigates. A `<button>` that changes the location is the lying-affordance class this repo has already ruled on twice (the crumb separator, the drawer close) | The control stops being navigation |
| `span.sp` — the strip's spacer, +95px wide | **Follows from the row above.** Not a decision | 2026-09-03 21:38 EDT | One button instead of two leaves the flex spacer wider. It has no ink | *Discard all* is adopted |
| **"across N realms"** — mk 4, pt 2 | **THE MOCKUP IS WRONG AND THE PORTAL IS RIGHT.** Class (b) | 2026-09-03 21:38 EDT | Both sides compute `new Set(ops.map(o => o.realm)).size`. `docs/superpowers/mockups/2026-08-23-portal-interactive/assets/shell.js`'s `Store.add` REWRITES `op.realm` from the string it was given into an object `{href, label}`, so the design's Set counts four distinct object identities where `FIX.sampleOps` holds `season · season · armory · season` — **two** realms. Same class as `t-legendary` and Broadcast's short `colgroup`: a package defect, not a design decision. ⚠️ It is the same object-versus-label fault Part 6a fixed on Review, one surface over | `shell.js` stops boxing the realm, or the design reads `.label` |
| The countdown's `hrs`/`min` figures and the widths of `div.sc-face` and `span.sc-u` | **CITED — the clock's time source.** Class (b) | 2026-09-03 21:38 EDT | The design counts from the START of its fixture day, so it reads `23:59:59` on every capture; the portal keeps `Date.now()`, which is the settled decision recorded in this file's Season section and is what a running console needs. `portalDiff` freezes both sides and they agree; `portalAudit` does not freeze, and now says so in its own stated coverage. Reported on every unfrozen run of any realm with a clock | The portal's clock stops reading the wall clock |
| The two `repair` rows' ORDER — pt Armory 66 before Season 16. ⚠️ **THIS IS A SYMPTOM OF AN OPEN ITEM, NOT A SETTLED DIFFERENCE** — the same commit that cited it filed the underlying count as `[P2 · S]` (*"three answers for one question: 13 · 60 · 66"*). It accounts for 4 of the 27 regions (R2, R3, R10, R11) and they close when the count does. Named 2026-09-03 23:27 EDT by the §L ⑥ agent | **Follows from Armory's own predicate.** Class (b) | 2026-09-03 21:38 EDT | Both sides sort by severity then count, and both `repair` rows share severity 60, so the order is decided by Armory's number. The mockup's `armoryFault` and the portal's `coverageFlags` are different predicates over the same builds. Home reads Armory's, which is the rule this row exists to protect | 🔴 **REWRITTEN 2026-09-03 23:27 EDT — the old falsifier could never fire.** It read *"Armory's coverage predicate changes"*, and since Home now IMPORTS `splitCoverage` from `armory.js` a predicate change moves both sides together and cannot invalidate this row. Every way the order actually flips left it silent. **It reopens if: Season's repair count rises above Armory's · a third `repair` row appears · the `SEV` ladder changes · or the fixture data moves either count.** Found by the §L ⑥ agent |
| Home's masthead — **three figures, not four** | **Design's three.** `days left` dropped | 2026-09-03 21:38 EDT | COMPANION §16.6 and §5.9z.5 both NAME four (days left · live now · staged · needs you) and the design RENDERS three. The mockup is the authority the pass is measured against, and the countdown sits ~200px below in Home's own clock panel, which states the same deadline and earns two item columns off it. Harkirat, 2026-09-03 21:38 EDT: conform to three now, revisit at the redesign phase — filed in `docs/db-deferred-list.md`. ⚠️ **Do not "restore" it as a missing figure** | 🔴 **REWRITTEN 2026-09-03 23:27 EDT — *"the redesign phase reinstates it on merit"* restated the deferral rather than naming an observable.** **It reopens if: the design's own `index.html` renders a fourth masthead stat · or Home's clock panel stops stating the deadline**, which is the condition that made a masthead countdown redundant in the first place. Found by the §L ⑥ agent |

### Home — the resting regions, mapped onto the cited set

*Added 2026-09-03 23:12 EDT because §L ② closes on the ENUMERATION and this table did not exist — the regions lived only in a gitignored working file, as nine class groupings. Review was given the equivalent table on 2026-09-03 09:07 EDT for the same reason. Caught by the §L ⑥ agent.*

`node scripts/portalDiff.mjs --realm home --portal harness --mk-query demo=1` → **0.7% of pixels, 27 regions, mk 1243px · pt 1243px** (re-measured 2026-09-04 12:40 EDT; this line said 25 and the last row of the table said 27 — the count moved 26 → 25 when region 13 was fixed and then to 27, and only the row noticed). ⚠️ **The tool prints the largest 14; the remainder are fragments of the same regions and it does not enumerate them** — a limit of the instrument, stated rather than claimed away, the same way the Analytics row states it.

| # | Region | What it is | Class |
|---|---|---|---|
| 1 | `span` "staged · review" ×2 | The header's commit crumb, same text both sides | sub-pixel |
| 2 · 3 · 10 · 11 | `b` and `span.att-sev` — "16 season items" ↔ "66 builds", "16 of 39" ↔ "66 of 133" | The two `repair` rows in the opposite ORDER, because Armory's predicate gives 66 where the design's gives 13 | CITED — Armory's own predicate, filed `[P2 · S]` |
| 4 | `input.cb-in` | The command-bar input, empty both sides | sub-pixel |
| 5 | `button.chip` "Discard all" ↔ `div.hres` | The design's discard verb, which the portal omits | CITED — `Resume`'s own reason |
| 6 · 8 | `b` "59"↔"18", "23"↔"05" | The countdown's minutes and hours | CITED — the portal keeps `Date.now()`; the design counts from its fixture day |
| 7 | `kbd` "⌘K" | Same glyph both sides | sub-pixel |
| 9 | `button.chip.go` ↔ `a.chip.go` "Review & commit" | Button versus anchor | CITED — it navigates |
| 12 | `span` "across 4 realms" ↔ "across 2 realms" | The mockup counts realms by object identity | CITED — a package defect; the portal is right |
| 13 | `b` "oldest up 19d" ↔ "20d" | ✅ **FIXED 2026-09-03 23:12 EDT** — the age truncated to calendar days where the design rounds real elapsed time, twenty lines below an `endsIn` corrected for exactly that an hour earlier. Instance fixed, class missed; this region found the other half | resolved, region count 26 → 25 |
| — | `dior` ↔ `…2283` — the account chip | The mockup's fixture viewer against the portal's real session id. Adjudicated as a cited privacy row on three realms; **uncited on Home until 2026-09-03 23:28 EDT**, when `--triggers` surfaced it as a one-sided control | CITED |
| 14 | `span` "Home" ↔ `span.crumb` "Portal Home" | The crumb's wording | CITED — and its comment's SECOND justification was false: `index.html` carries no crumb at all. Corrected in `portal/ui/shell.js`; the decision stands on its surviving half |
| new | `span.lw` — "2 days left" ↔ "ends tomorrow" | **Portal keeps CALENDAR DAYS.** The design rounds elapsed time against midnight, which is wrong at both ends of its own range: an announcement expiring TODAY at 18:00 reads "ends tomorrow", and one that expired YESTERDAY reads "ends today" — on the panel whose subject is what players see right now. Measured 2026-09-03 23:20 EDT | CITED — reopens if the design's `endsIn` stops rounding |
| — | not printed | ⚠️ **The tool lists the largest 14 and there are 27.** It said "Every region is CLOSED or CITED" unconditionally until 2026-09-03 23:20 EDT — a string literal, printed after truncation, over regions no reader had seen. It now states what it withheld. ✅ **ENUMERATED 2026-09-03 23:28 EDT VIA `--json`.** All thirteen are 16×16 to 48×32 and cluster in two places: **x=16–32, y=64–320** (five — the nav rail's icons, sub-pixel) and **y=224** (six at x=224…592 — fragments of the staged strip's "across N realms" line, which is region 12), plus (1136,592) and (352,336). **The tool's claim that the remainder are fragments IS true here** — but it was printed unconditionally as a claim, and it is checked now | verified fragments |

### Cross-realm additions from the §L ⑥ overlay run, 2026-09-03 23:28 EDT

| Surface | Decision | Why | Reopens if |
|---|---|---|---|
| **Access · `+ Grant access`** — the design opens a DRAWER, the portal scrolls to an INLINE form | **Portal keeps the inline form.** Class (b) | `access.js`'s `onClick` is `setView('By admin'); scrollToGrant()` and its own tooltip says *"Jump to the grant form"* — the form is already on the page. `--open` reported *"clicking … opened nothing"*, which reads as a dead button and is not: **the node-count heuristic cannot see an affordance that SCROLLS rather than MOUNTS.** A blind spot in the instrument, now stated in the plan | The grant form becomes conditional, or the design's drawer is adopted |
| **Armory · the create overlay** | **First measured 2026-09-03 23:28 EDT** — `--open-sel ".mh-add button"` → ② 156 · ③ 15 · ④ 146, portal +1990px | `--open "New MP build"` refuses on BOTH sides because the two spell the control differently (`New MP build` vs `New MP build B`, the portal's shortcut chip). `--open-sel` is the escape hatch and no carrier named it | — |

### Home — what has NOT been measured, stated so nobody assumes coverage

The instruments state their own blind spots on every run and a ledger section should do the same. As of 2026-09-03 22:32 EDT, Home has been compared at 1282×888, at rest, with the clock running, on fixtures:

- **No overlay has been opened on Home, on either side.** `--triggers` lists what exists; `--open` has not been run here.
- ✅ **`--hover` and `--focus` HAVE now been run** (2026-09-04 12:40 EDT), on Home and on all six other realms — and the reason they had not is that **`portal:audit` never applied either flag**; they were declared and unused. `portalDiff` had them wired, which is what made the pair look covered. ⚠️ **A `--hover`/`--focus` run widens the walk past `main`**, so its section counts are not comparable to a resting run's — Home reads ② 8 at rest and ② 28 under either flag, and all 20 extra rows are the header and the rail.
- ✅ **Home HAS now been loaded against the real server (`:8787`)** — §L condition ⑤, run 2026-09-04 12:40 EDT: past the door, 4 realm nodes, 4 rows, one favicon 404, against a **freshly restarted** server. It fetches seven endpoints, two of them expensive (`/api/analytics` assembles seven fields from six collections; `/api/access/matrix` builds every admin × every scope) and they answer. ⚠️ **It had been recorded as owed for days and was in fact UNRUNNABLE** — `portalRealWalk`'s past-the-door probe counted `main tr, main .bar, main li, main .rec-row, main .bcard` and Home draws none of them.
- **375×812 has never been run on any realm**, and Home's block order changed this session — the change most likely to break a responsive stack.
- **Tab order was not checked.** Moving the masthead inside `.home` changed DOM order; `portalStates` counts focusables (16) but a count is not an order.
- **A delegated admin's Home is undesigned.** A realm the admin cannot see answers `forbidden`, and its attention row simply does not appear — so a delegated admin sees a smaller `needs you` figure with nothing saying why. Armory's masthead has an explicit ruling that a figure which cannot be known must not read as zero; this list makes the opposite choice by omission. Filed.

### Home — what was FIXED rather than cited

| Fix | Evidence |
|---|---|
| The masthead rendered OUTSIDE `.home`, so `.home .masthead` and `.home h1` matched nothing | `portal:probe` reports `w=1080 h=129 x=139 y=52` on both sides, "every requested property agrees" |
| `ClockFace` still held the retired hero clock, and Home was its only consumer | One shared face; `.sc-hero`/`.sc-heroL`/`.sc-unit`/`.sc-tick`/`.sc-t-sep` deleted; the design's `.sc-u:first-child b` tiers adopted |
| The attention list emitted ONE severity kind of five, and never queried Access or Analytics | Five rows, five kinds, matching the design's ladder and its copy verbatim |
| The row read `bpEnd` where the design reads the season's LAST deadline | `seasonConflictCount` — Track's own corrected edge |
| Home held its own copy of Armory's fault predicate and of Season's Repairs count | `splitCoverage` and `seasonRepairCount` imported from the realms that own them |
| `seasonItems` collapsed an ALL-SEASON row to one day and folded draw windows into Events | `isOngoing` resolved to the season end; `calCategoryOf` drives the lane |
| `endsIn` said "ends in 2d" and had no `ends tomorrow` | The design's three rungs, rounding the real remaining time |
| **Block ORDER** — the staged strip sat 1,281px below where the design puts it | masthead → resume → clock → attention list → live. `portal:converge` went from a flat +414px offset on eleven nodes to +0 everywhere |
| `.hclock .sclock` was never defined, so every clock segment on Home rendered right-aligned | The design's own rule, adopted |

## Overlay tier — one row per overlay, graded on the DELTA

> 🔴 **The close condition is the plan's ⑧.** A row here is the overlay's own contribution — the realm's resting `--all` subtracted from its `--open` run — never the page-sized figure the instrument prints. The three standing subtractions (`② 9`, `⑤ RULES`, a refusal on an unchanged node count) are in ⑧ and are not repeated per row.

| Realm · overlay | Delta ② / ③ / ④ | State | Reopens if |
|---|---|---|---|
| season · composer, six kinds · export drawer · day drawer · identity editor · record preview | 0.2–1.2% each | **Closed** — Part 1, enumerated | A composer kind is added |
| armory · `Export…` | 26 / 4 / 51 | **Adjudicated by class** — the shared export drawer. See the row below | — |
| armory · create (`#mhAdd button:nth-of-type(2)`) | — | ✅ **FIXED, not cited.** The portal switched the rack instead of mounting a form; Harkirat ruled the mockup right | The two armouries stop being separate racks |
| broadcast · `Export…` | 28 / 5 / 39 | 🔬 **TRIAGED MEMBER BY MEMBER — the reference case for all four export drawers.** 18 of its ② are the settled icon class, the whole-page pairing node and the cited `span.id`; **the remaining 7 were the staged tray, missing from the portal entirely** — now built. Its ③ is the crumb, the identity button, the `✕` glyph, the two cited export-scope notes and the cited `p.expnone`. Its ④ is `main`'s reserve for the tray (reach 234, the largest row), the cited identity widths, the cited crumb separator and the 9 rows of Harkirat's commit-chip decision. **Nothing was left unaccounted for** | The export drawer is redesigned |
| broadcast · `+ Post announcement` | 28 / 5 / 43 | **Enumerated; same three classes as the row above plus the post form** | — |
| access · `Export…` | 22 / 6 / 48 | **Adjudicated by class** — the shared export drawer | — |
| access · `+ Grant access` | — | **CITED, not work.** The design opens a drawer; the portal does `setView` + `scrollToGrant`, which Harkirat chose. `--open` refuses because the node count does not change — that refusal is the evidence | The inline form is replaced by a drawer |
| analytics · `Export…` | 18 / 7 / 61 | **Adjudicated by class** — the shared export drawer | — |
| analytics · alert-level chips (`--open-sel ".lvlb"`) | 8 / 2 / 4 | **Closed.** The smallest delta in the tier: the chips filter the river in place rather than opening anything, so the delta is the three standing subtractions and nothing else | A chip starts opening a surface |
| review · `Discard all` | 18 / 4 / 43 | **Enumerated.** Every member falls in a named class: the icon set, the pairing nodes, the cited identity row, the cited crumb, and the drawer geometry that follows from the copy | — |
| season · the one-way typed confirm | — | **CITED — it cannot be compared.** The mockup draws `Export first →` ×5 where the portal draws the seven real one-way operations; `--triggers` lists `Purge every draw…` as ONLY IN PORTAL. There is no counterpart to open, and that IS the already-cited `oneway.js` decision | The design gains the seven operations |
| home | — | **None exist.** Home commits nothing and has no composer — `--triggers` lists only the rail and the view chrome | Home gains a composer |

🔴 **THE FIGURES ABOVE ARE POST-SEEDING AND THE EARLIER SET IS SUPERSEDED — 2026-09-04 21:41 EDT.** Every realm became a SEED realm when the rail badge and the commit chip started rendering on all seven, so every resting baseline moved and every delta taken against an older one is wrong. The four `Export…` deltas are graded as ONE finding by §0.7a's own rule — *a repeated pattern is one finding* — because they are four instances of `exportPanel.js`, and Broadcast's is triaged member by member as the reference. ⚠️ **That is a real inference and it is stated as one**: if `exportPanel.js` ever renders differently per realm, the three by-class rows are unadjudicated again.

## ⑥ UX-COPY — the phase that had no observable, worked 2026-09-04 20:53 EDT

> 🔴 **The plan's own audit log calls this "the most-missed body of work" in the project.** `local/handoff/2026-08-25-portal-ux-copy-audit.md` (gitignored — state the path) existed for ten days and had never been folded into any Part, and the reason is structural: its findings are prose, so *"apply them"* is unfalsifiable and a session can believe it complied. **The mechanical half is a gate now** — `npm run portal:uxcopy`, in `npm test`. The judgement half is these rows.

| Audit row | In the portal? | What was done |
|---|---|---|
| **A1** staged work survives sign-out, or is destroyed by it — five contradicting strings | **No** | The portal already says it survives, in all three places: the door, the expired banner, and the sign-out confirm (`shell.js:409`'s comment records the reasoning). The contradiction is the mockup's |
| **B** source paths, model names and `foo()` printed to the reader | **Yes, 7 of them** | Fixed, and the class is now a gate. Analytics named four collections to answer a question about WHAT is counted; Armory printed a bot source path and a parser function; Review titled two panels with the model name |
| **C1** "1 GATES OPEN" | **No** | `review.js:159` already reads `blocker` / `blockers` and paints it warn. The design's string is the defect |
| **D2** the `(s)` plural | **Yes, once** | Armory's bulk-badges label and placeholder. The count is known at render time, so the word agrees now. Gated |
| **Vocabulary — "changeset"** | **Yes** | Review's two panel headings read **Staged changes**, which is what every other surface already calls it. `Changeset` is this product's model name and a git word besides |
| **E1** the fallback banner *"Something is wrong."* with an empty explanation | **No** | `async.logic.js` gives every banner kind a real `what` and `means`. The mockup's default is the defect |
| **B/§F/§G** — judgement rows: copy that justifies its own existence, design principles aimed at the wrong reader, dated history in the UI | **Mostly the mockup's** | Not ported wholesale. ⚠️ **Not exhaustively re-checked against the portal** — the gate covers the four mechanical classes and nothing more, which it says in its own output |

⚠️ **What the gate cannot see, so that nobody reads its green as coverage:** copy composed at runtime from variables, copy in `portal/api`, and every judgement class above. Those are hand-worked and this table is the record.

## Part 7 — the sweep, the misc, and the double-check

*Written 2026-09-04 23:46 EDT. These are the rows the SEVEN realm sections could not hold, because each is about something that is true on every realm at once or true of neither page. **The reverse-orphan sweep is the only instrument in the suite that looks for a rule no element reaches and a value nothing supplies**, so every row here was invisible to the diff, the audit, the converge and the probe — by construction, not by accident.*

| Surface | Decision | Date | Why | Reopens if |
|---|---|---|---|---|
| `--mono` (4 reads) and `--focus` (1 read), set in neither token file | **FIXED, in BOTH.** `--mono:var(--data)` and `--focus:#5FD4E8` | 2026-09-04 23:46 EDT | A `font` shorthand whose family resolves to nothing is **invalid at computed-value time**, so the whole declaration is dropped — the size and the weight go with the family. `.fail-d`, `.scope .nm em`, `.holder` and `.reph .rt-n` were rendering at whatever they inherited. 🔴 **The package reads the same unset token in the same four rules**, so both sides were wrong identically and every pixel instrument read zero. Corrected in the package under §0.1a test 2: a sheet that reads a token its own token file never sets is a DEFECT, not a weakness to reproduce. `--focus` always carried its literal fallback beside it, so it rendered; setting it changes no pixel and removes the lean on a repeated literal. Measured after: `portal:sweep` reproduces all seven floors unchanged; Access and Season geometry moved and were re-recorded in the same commit, `hs>holder|w` closing and size issues 7 → 6 — **the fix showing up as an improvement in a fixture is the evidence it did something** | Either token file stops declaring them, or a fifth rule reads a token nothing sets — `npm run portal:reverse-orphans` ② is the standing check and it is 0 for the first time |
| `.preview-sel` on the Manifest row whose preview drawer is open | **REMOVED.** Class (a) — a portal-only emission no sheet paints | 2026-09-04 23:46 EDT | Emitted on the Manifest row tag (cited as `manifest.js:236` when found; the line no longer exists, and the removal comment sits in its place) and matched by no rule in `app.css` or in the package's own sheet, so the one row a reader most needs located looked exactly like its neighbours. §0.6a says the portal renders the mockup's version until every realm matches, and the mockup marks nothing there — styling it would be inventing design mid-pass. ⚠️ **The state is real; the design's silence about it is a WEAKNESS**, filed in `docs/db-deferred-list.md` rather than fixed. ⚠️ **The comment recording this was first written as an HTML comment INSIDE the tag's prop list**, where htm swallows every prop after it: it would have removed `tabIndex`, `onKeyDown` and `onClick` from every Manifest row on every realm. The `${''/* … */}` form two lines below is the file's own idiom for exactly that reason | A mark for the open row is designed, on either side |
| `.lone` — a permission scope no non-owner holds | **KEPT, unstyled.** Reproduced, not corrected | 2026-09-04 23:46 EDT | The portal emits `scope lone` and paints nothing; **so does the package** (`access.html:258` computes the same `flag` and its sheet has no `.lone` either). The meaning is carried in words by `.flag.quiet` — *"nobody but you"* — on both sides, so the class is a vestigial hook rather than a lost state. §0.1a: a weakness the package shares is reproduced and filed. ⚠️ **The reflex here was to delete it as dead**; checking the other side first is what stopped that, and it is the same rule that keeps catching LCS pairing artifacts | Either sheet grows a `.lone` rule, or the flag copy stops naming the state |
| `[data-src]` on `.who .av`, styled on both sides and emitted only by the package | **DELIBERATE on the portal. CITED, not a gap** | 2026-09-04 23:46 EDT | `shell.js:217` is the receipt and predates this pass: `GET /auth/csrf` returns `discordId`, `isOwner` and `visibleRealms`, and **nothing in this codebase stores a username or an avatar hash**. Rendering the design's markup would emit `url(undefined)` — a real network request from a page whose whole premise is that it asks for nothing it did not say it would. `--av-src` is set to `none`, which is a VALID value, so the rule falls through to its designed ground instead of being dropped | The portal starts storing an avatar hash |
| `[data-role=editor]` on `.mx tbody tr`, two rules on BOTH sides, emitted by neither | **Inherited package weakness. Reproduced and filed** | 2026-09-04 23:46 EDT | The design draws an editor-role admin's granted cells differently (transparent fill, accent border, and a `::after`) and never emits the attribute that would select them, so the state exists in both stylesheets and on neither page. Building it on the portal alone would put the portal ahead of the design in the middle of a conformance pass, which §0.6a forbids until every realm matches | The design emits the attribute, or the role distinction is designed for the portal after conformance closes |
| The manifest-cell kind API in `tokens.css` — a class qualified by a `date` / `id` / `code` kind | **REMOVED.** Portal-invented, never grown | 2026-09-04 23:46 EDT | A repo-wide search returned the two selector lines and no others, on either side. The real Manifest types its cells through `columns[].kind` in `manifest.js`. ⚠️ **The four selectors left in that rule are ALSO unemitted and were deliberately kept** — `.mono`, `.track-ruler`, `.track-date`, `.board-id` are single-rule selectors, so ③ (which needs ≥2 rules) cannot see them and ① does not cover a class. Naming them in the comment is the honest half; widening the deletion to four things to make a point about one is the over-wide slice the deletion rule warns about | Any of the four gains a second rule and ③ starts reporting it |
| **Item M#4** — a component clipped to 1px by a class-name collision | **It was solved in August and the tracker was never told.** A RECORD, not a decision | 2026-09-04 23:46 EDT | The season record was named `.sr`, which is the screen-reader-only utility (`position:absolute;width:1px;height:1px;clip:rect(0 0 0 0)`). It inherited all four, so the component rendered for screen readers and was 1px tall for everyone else — no error, markup in the DOM, `innerText` reading back perfectly. Renamed `.srec`. **The plan carried it as "Never identified" while a twelve-line comment three lines from the fix explained it in full.** Same shape as the hero clock deleted at its call site and recorded as deleted from the class: a fix that lands without closing its own tracker row is indistinguishable from one that never happened | Nothing. It is gone — a record correctly has no falsifier |

### The four shared-chrome changes of `a9b8357e`, cited late

🔴 **Written 2026-09-04 23:56 EDT, and the lateness IS the finding.** `a9b8237e`'s own subject line says *"four shared-chrome defects, each one edit reaching seven realms"* — and it wrote **zero** ledger rows, while the subtraction pass two commits later called itself *"six deletions"* and wrote six. **The commit with the larger blast radius is the one that recorded nothing.** The cause is legible: a change that FEELS like a repair does not feel like it needs a decision row. But three of these four alter what the portal DRAWS relative to the package, and that is a decision however it felt. The rule this produced is in the plan's §0.6.

| Surface | Decision | Date | Why | Reopens if |
|---|---|---|---|---|
| `.masthead` `padding-left` 23px → 22px, every realm | ✅ **PACKAGE DEFECT, corrected in BOTH** | 2026-09-04 23:56 EDT | `.panel{margin:0 22px}` and `.identity{margin:0 22px}` — the masthead's 23 was the single outlier, so the realm title, the largest type on the page, sat 1px off the column its own panels define. §0.1a test 2: a sheet disagreeing with its own system is a defect, not a weakness to reproduce. ⚠️ **It was portal-only until tonight**, which meant a future ④ STYLE row reading `23px → 22px` had two equally plausible stories — the portal drifted, or the portal corrected — and the evidence that settles it lived only in a commit message | The package's column system moves off 22px |
| `.mh-stats .v` declaring `letter-spacing` twice | 🔴 **THE PORTAL RESOLVED IT THE WRONG WAY AND HAS BEEN REVERTED** | 2026-09-04 23:56 EDT | The package writes `--tr-fig` (+.004em) then `--tr-figure` (−.02em) in one rule. **Source order means `--tr-figure` is what the design has always rendered**, so deleting it and keeping `--tr-fig` moved every masthead figure on every realm 0.024em away from the design — a portal-ahead judgement taken mid-pass, which §0.6a forbids until all seven realms match. Reverted to the value the design renders, as ONE declaration in both sheets: same pixels as the package, no dead line in either. ⚠️ **The preference is real and is filed, not lost** — `.tile .tl-v` uses `--tr-fig` for the same face at the same size, so the two figure voices genuinely disagree. That is a post-conformance question | The two figure voices are unified deliberately, after conformance closes |
| `header .hdr-commit{display:none}` below 768px | **Portal keeps it.** Class (c) | 2026-09-04 23:56 EDT | Measured: the chip is destroyed at 375px on all seven realms — `clientWidth` **0**, a 2 × 70.25 rect hanging above the header — because the header is a flex row with no shrink floor and the chip is the first thing crushed. Hidden for the same reason the crumb already is, and the rail badge carries the count. ⚠️ **The package has no counterpart rule**, so below 768 the two pages differ by a whole control. That viewport is deliberately out of scope (Harkirat, 2026-08-27), which is why this is cited rather than chased — **but "out of scope" is not "identical", and the row exists so nobody reads the silence as agreement** | 375×812 comes into scope, or the header gains a shrink floor |
| The word `EXPORT` beside a button reading `Export…`, six realms | **Portal keeps the removal.** Class (b) | 2026-09-04 23:56 EDT | The package's shared shell builds `<span class="mh-add-k">{label}</span>` inside `.mh-take` (`assets/shell.js:2634`); the portal's `exportPanel.js` no longer does. A label naming the component, immediately left of the component. The group still carries it as its accessible name, which is where it does work. 🔴 **This is user-approved work, not drift** — it came out of the six-reviewer design pass Harkirat commissioned, whose single converged finding was that *nothing is ever taken away*. ⚠️ **It is worth ~57px**, and it is the mechanism behind `div.mh-take w -57` in `portal:converge` on Season and Broadcast and the `.mh-id` / `.mh-stats` width redistribution above it — a difference that costs **zero pixels in the diff**, because the width it frees is painted the same colour as the ground | The design's own export strip drops its label, or the redundancy argument is reversed |


## Superseded 2026-09-06 — the build-out

> 🔴 **THE STAND-DOWN RULE IS DEAD, and every resting-frame floor above is HISTORY from this section on.** Harkirat, 2026-09-06 00:43 EDT: *finish the portal; the conformance is more or less done, it just needs correcting; don't feel restricted by prior decided decisions.* The rule that redesigns return only after all realms match (2026-08-28, re-affirmed 2026-08-31) is superseded by that message. **A moved floor is not a regression.** `portal:diff` and `portal:audit` still run and still measure; what they report against the package is now a list of deliberate divergences, and this section is where each one is cited. Written 2026-09-06 01:26 EDT.

**The eight decisions the build-out rests on** (taken 2026-09-05/06, popups; do not re-ask): D1 creation forms are modal drawers on every realm — *overturns the class-(b) row above, "Portal keeps the inline panels"* · D2 draw window is not a creation kind; a draw's optional end date stages a calendar op beside the draw op · D3 the identity chip shows username + avatar, with PRIVACY.md §2.1c's portal-session cell amended · D4 distinct hue per realm, Review = ink, Home = ink2 · D5 Armory: rack collapsed by category, Compare is a weapon search · D6 `layout` + `typeset` released, seven realms at once · D7 the impeccable design hook is wired and filed · D8 DESIGN.md Components + seven surface briefs.

### Unit A — the design system (tokens, app.css, manifest)

| Surface | Decision | Date | Why | Reopens if |
|---|---|---|---|---|
| UI type scale | ✅ **PORTAL AHEAD** — `--t-micro 9.5 · xs 10.5 · sm 12 · base 13 · md 14.5 · lg 16.5 · xl 19 · hero 26`; 316 literal px sizes in `app.css` mapped onto steps; 17/20px stay literal on the Armory rank column; the Discord preview keeps Discord's sizes | 2026-09-06 01:26 EDT | D6 `typeset`. `undersized-ui-text` was the detector's top rule (+171 portal-side, 1009 on the design's own Armory) and pin 14 named the air | The package adopts the scale, or a realm's nowrap column overflows at +0.5px (none seen on the harness at 1282) |
| Spacing scale | ✅ **PORTAL AHEAD** — `--s1…--s6` (4/8/12/16/24/32); masthead `36 22 26`, `.mh-stats` s4, `.panel + .panel` s5, `.rec` s5/s4, `.identity` s5, `.ph` `10px 14px` | 2026-09-06 01:26 EDT | D6 `layout`; pin 14 | A structural gap is written as a literal again |
| Realm accents | ✅ **PORTAL AHEAD** — `--r-broadcast:#E56FA6 · --r-access:#6B8AF7 · --r-analytics:#9CC85A · --r-review:var(--ink) · --r-home:var(--ink2)`; season and armory unchanged | 2026-09-06 01:26 EDT | D4. Four of six aliased state colours, so topic and state shared a hex on four realms | Harkirat sees the hues live and wants a different set — the lime and the indigo are mine |
| Masthead wash | ✅ **PORTAL AHEAD** — a 10% realm-hue radial at the top-left of every masthead, keyed on `.app[data-realm]` (unit D emits the attribute); none on Review and Home | 2026-09-06 01:26 EDT | `bolder` — the signature; a light the page is lit by, not a coloured box | It reads as a box; then lower the alpha, not the idea |
| Topic side-tab | ✅ **PORTAL AHEAD** — 2px on the 8 topic cards (`.bcard`, tier cards, composer preview); state callouts keep 3px | 2026-09-06 01:26 EDT | `quieter`. `side-tab` fires 59× on the mockup, 29× on the portal; where the border IS the message it stays | — |
| `.dwfield label` | ✅ **PORTAL AHEAD** — sentence case, `--t-sm`, `--ui`, no tracking; `.dwfield textarea/select` take the input's skin; `.dw-f .why` is the reason line; `.dw-grid2` | 2026-09-06 01:26 EDT | D1 makes a six-field drawer the common case, and a six-field form is read, not scanned | — |
| Manifest remove column | ✅ **PORTAL AHEAD** — `td.ra` 52px / 12px in, `.rmv` ghost ring at rest, `data-tip` on hover | 2026-09-06 01:26 EDT | Pin 22 | — |
| `.rec-h` / `.rec-cta` | ✅ **PORTAL AHEAD** — `align-items:center`, 30px inline-flex CTA at `--t-sm` | 2026-09-06 01:26 EDT | Pin 8 — the CTA hung 8px under a 10.5px eyebrow on a baseline row | — |
| `.rec-mk` | ✅ **DEFECT, fixed** — `justify-self:center` | 2026-09-06 01:26 EDT | Pin 9 — the spine runs at `--rec-x` and the marker sat at its column's left, 4.5px off the line | — |
| Page-load motion | ✅ **PORTAL AHEAD** — masthead figures rise 60ms apart, rail items stagger once, on `--dur-3`; zeroed under reduced motion | 2026-09-06 01:26 EDT | `animate`; one orchestration, not scattered effects | — |
| Pins 15/20/33/35 (colour missing on the LIVE site) | ⚠️ **NOT REPRODUCED, and the stale-API story is FALSE** — the `:8787` process started 2026-09-05 11:22, after the `realm`-emitting route landed (2026-09-01); the server serves `portal/public` from disk per request, so what he saw was whatever build was on disk at 23:44. The dev DB stores lowercase full tier words, which `TIER_WORD_CLASS` maps; `realmForScope` maps every `manage.<page>` scope | 2026-09-06 01:26 EDT | The surviving explanation is a disk build older than #183; unverifiable now | He sees them uncoloured again after this branch's kickstart — then it is code, and the instrument is `curl` of `app.css` beside the harness |

## Instruments — what they can and cannot do

> 🔴 **TWO ALIGNMENT FAULTS FOUND IN PART 6b, 2026-09-03 21:38 EDT — both made the instruments compare different DATA and report it as different DESIGN.** · **The harness stood on the real date.** `fixtures.js` pins `F.today` to `2026-08-24` but stamps `documentElement.dataset.today` only when the URL carries `?today=`. Every portal component reads that value and otherwise falls back to the wall clock — so under `portalDiff`, which freezes `Date`, the two sides agreed, and under `portalAudit`, which does not, the portal was rendering **ten days ahead of the design**. Home's "Running right now" listed five entirely different items per side. The harness page now stamps the fixture day itself, deferring to an explicit `?today=`. · **The staged seed reached one page.** `loadSample()` lived inside `review.html`, so `?demo=1` worked on Review and nowhere else — while the portal harness synthesises four changesets on every page. It is `seedDemoOps` in the mockup's shared `docs/superpowers/mockups/2026-08-23-portal-interactive/assets/shell.js` now, and **the seeding refusal covers Home as well as Review** in all five instruments. Seeded, Home's two pages are the same height; unseeded they were 78px apart. ⚠️ Placing the CALL beside the Store was a TDZ error that blanked every page in the package — `node --check` passes it, because TDZ is a runtime fault.

| Instrument | State | Reopens if |
|---|---|---|
| `portalGeometry` | **Clock frozen 2026-08-31**, storage cleared. It was the last one that was not | — |
| `portalInventory` · `portalStates` | Clocks frozen the same day | — |
| `portalAudit` · `portalDiff` · `portalProbe` · `portalConverge` | Freeze and clear storage. ⚠️ **`--at` must stay at the mockup's own `F.today`, `2026-08-24`** | — |
| `portalCaptureModes` | **Historical.** No conform-OFF rendering left to photograph; its arrival assertion now refuses every run, which is correct | — |
| **The triage classifier** | ⚠️ **Regex classification, NOT measurement.** Can report a **false CITED on any `ow-*` row** | — |
